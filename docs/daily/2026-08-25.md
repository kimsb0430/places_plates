# 2026-08-25 Daily Report

## 한국어

- 목표: Cloud Run 리비전 교체와 다중 인스턴스에서도 관리자 로그인이 유지되도록 서버 세션을 PostgreSQL에 영속화하고, C14 비공개 정제 마스터 파이프라인을 구현한다.
- 완료: Spring Session JDBC 의존성과 Flyway 전용 스키마 초기화 설정을 추가했다. V9에서 표준 세션·속성 테이블과 인덱스를 만들고 V10에서 `placesplates_app` CRUD만 허용하며 `PUBLIC`·Supabase Data API 역할 접근을 차단했다. 인증 principal을 직렬화 가능하게 변경하고 실제 `SESSION` 쿠키 기반 MVC 테스트와 새 저장소 인스턴스의 인증 복구 테스트를 추가했다. 프로비저닝 검증은 V10 이력, 런타임 세션 권한과 공개 역할 차단까지 확인한다. 운영 V9·V10은 정상 적용됐고, 비밀번호 갱신 직후 Session pooler의 `28P01` 인증 지연에는 제한 재시도를 추가했다. 병합 커밋 `42488b6`을 Cloud Run `places-plates-api-00020-lkn`으로 배포해 트래픽 100%를 전환했다. 이어서 C14에서 인증된 처리 API, 업로드 항목별 작업 잠금, Supabase 비공개 객체 읽기·정제 마스터 저장, JPG·PNG 방향 보정과 JPEG 재인코딩, EXIF·XMP·IPTC 재검사, `photos`·`photo_assets`·업로드 결과 연결을 구현했다. 프론트엔드는 업로드 직후 정제를 호출하고 성공·실패·재시도와 HEIC JPEG 변환 안내를 표시한다. C14 병합 커밋 `140abbb`은 Vercel과 Cloud Run에 자동 배포됐으며, Cloud Run 최신 리비전 `places-plates-api-00022-pw5`에 트래픽 100%가 전환됐다. 가짜 GPS·카메라 XMP가 든 합성 JPG 운영 업로드는 새 비공개 여행지 초안으로 이동했고 정제 작업 `COMPLETED`, 비공개 `SANITIZED_MASTER`, `metadata_scan_passed=TRUE`를 확인했다. 이 과정에서 사진 상태가 `PROCESSING`에 남는 누락을 발견해 성공 시 `READY`로 전환하고 완료 요청 재호출 시 기존 사진 상태도 복구하도록 보완했다. 보완 커밋 `a39f2d8`은 Cloud Run `places-plates-api-00024-ltz`에 배포되어 트래픽 100%를 받는다. 기존 사진은 UI 재호출 경로가 없어 `PROCESSING`에 남았으므로, 완료 작업·비공개 정제 마스터·메타데이터 검사 통과 조건을 모두 만족하는 기존 행만 `READY`로 바꾸는 V11 백필을 추가했다. 운영 프로비저닝에서 V11 한 건을 적용했고 대상 사진을 `READY`로 전환했다.
- 중요 파일: `backend/build.gradle.kts`, `backend/src/main/resources/application.yml`, `backend/src/main/resources/db/migration/common/V9__create_jdbc_session_store.sql`, `backend/src/main/resources/db/migration/postgresql/V10__secure_jdbc_session_store.sql`, `backend/src/main/resources/db/migration/common/V11__backfill_ready_sanitized_photos.sql`, `backend/src/main/java/com/placesplates/domain/auth/service/AdministratorPrincipal.java`, `backend/src/main/java/com/placesplates/domain/photo/service/ImageSanitizationService.java`, `backend/src/main/java/com/placesplates/infra/image/JavaImageSanitizer.java`, `backend/src/main/java/com/placesplates/infra/storage/SupabasePrivatePhotoStorage.java`, `backend/src/main/java/com/placesplates/infra/persistence/SupabaseDatabaseProvisioner.java`, `frontend/src/domain/photo/components/photo-uploader.tsx`, `backend/src/test/java/com/placesplates/infra/image/JavaImageSanitizerTests.java`, `backend/src/test/java/com/placesplates/infra/persistence/DatabaseMigrationTests.java`, `backend/src/test/java/com/placesplates/infra/persistence/JdbcSessionPersistenceTests.java`, `docs/AUTHENTICATION.md`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`.
- 검증: 병합된 `main`의 Verify·Secret protection과 `scripts/verify-all.ps1`이 통과했다. 비밀 검사, 프론트 lint·타입 검사·Vercel 및 Sites 빌드, 두 공개 산출물 검사, 백엔드 전체 테스트와 배포 JAR 생성이 성공했다. 백엔드 테스트는 JDBC 세션 저장·복구, 쿠키 기반 로그인·로그아웃, 데이터베이스 마이그레이션과 프로비저닝 계약을 확인한다. 운영 재실행에서 `PASS: Supabase database provisioned; 0 migration(s) applied in this run.`을 확인해 Flyway v10과 런타임 역할 검증을 완료했다. C14 병합 후 GitHub Verify·Secret protection, 병합된 `main`의 전체 로컬 검증, Vercel HTTP 200, Cloud Run 상태 API HTTP 200과 `{"status":"UP"}`를 다시 확인했다. Cloud Run 콘솔에서 `places-plates-api-00022-pw5`가 최신 리비전이며 트래픽 100%를 받는 것도 확인했다. 새 로그인 후 관리 화면 새로고침에서도 `관리자 세션 활성` 상태가 복구됐다. 운영 합성 JPG는 53,508바이트에서 정제 JPEG 54,674바이트·800×500으로 재인코딩됐고 UUID 전용 저장 키, 민감 메타데이터 검사 통과를 확인했다. READY 보완 병합 커밋의 GitHub 검사와 전체 로컬 검증, Vercel 배포, Cloud Run `places-plates-api-00024-ltz` 트래픽 100%, 상태 API `{"status":"UP"}`를 확인했다. V11 테스트는 완료·비공개·검사 통과 사진만 READY로 변경하고 검사 미통과 사진은 PROCESSING에 유지함을 확인한다. 운영 실행에서 `PASS: Supabase database provisioned; 1 migration(s) applied in this run.`을 확인했으며, Supabase `flyway_schema_history`의 V11·대상 사진 `READY`·조건부 백필 잔여 0건을 확인했다. Cloud Run `places-plates-api-00025-g2t` 트래픽 100%, Vercel HTTP 200, 상태 API HTTP 200과 `{"status":"UP"}`도 재확인했다. 기존 Next.js `metadataBase` 경고만 남아 있다.
- 결정: 세션 테이블은 사용자 소유 데이터가 아닌 서버 인증 인프라이므로 RLS를 사용하지 않고 제한된 런타임 역할의 CRUD와 공개 역할 권한 제거로 보호한다. 운영에서는 `spring.session.jdbc.initialize-schema=never`를 유지해 스키마 변경을 Flyway에만 맡긴다. 이미지 정제는 Cloud Run의 상시 백그라운드 CPU에 의존하지 않고 인증된 업로드 흐름에서 요청 단위로 실행하며 작업 행 잠금으로 중복 처리를 막는다. 정제 마스터는 2,500만 픽셀 한도, 품질 0.92 JPEG, 비공개 `sanitized/` UUID 키를 사용한다.
- 위험: 첫 운영 실행의 런타임 연결 검증은 Session pooler 인증 정보 전파 지연으로 실패했지만 제한 재시도 적용 후 최종 `PASS`를 확인했다. 현재 JVM은 HEIC·HEIF 픽셀 디코딩을 지원하지 않아 안전한 실패와 JPEG 변환 안내를 제공한다. 운영 스모크가 만든 테스트 초안은 비공개로 남아 있다. V11 백필은 적용됐으며 자동 하향 마이그레이션은 제공하지 않는다. 애플리케이션 롤백 지점은 직전 리비전 `places-plates-api-00024-ltz`이다.
- 다음 작업: 새 합성 JPG에서 신규 READY 전환과 세션 지속성을 다시 검증하고 C15 반응형 이미지 파생본·썸네일 생성을 시작한다.

## 日本語

- 目標: Cloud Runのrevision交換と複数instance間でも管理者loginを維持できるようserver sessionをPostgreSQLへ永続化し、C14非公開sanitized master pipelineを実装する。
- 完了: Spring Session JDBC依存関係とFlyway専用schema初期化設定を追加した。V9で標準session・属性テーブルとindexを作成し、V10で`placesplates_app`のCRUDだけを許可して`PUBLIC`・Supabase Data API roleのアクセスを遮断した。認証principalをserialize可能にし、実際の`SESSION` CookieによるMVC testと新しいrepository instanceからの認証復元testを追加した。provisioning検証はV10履歴、runtime session権限、公開role遮断まで確認する。本番V9・V10は正常適用済みで、password更新直後のSession pooler `28P01`認証遅延へ限定retryを追加した。Merge commit `42488b6`をCloud Run `places-plates-api-00020-lkn`として配備し、traffic 100%を切り替えた。続いてC14で認証済み処理API、upload item単位のjob lock、Supabase非公開object読込・sanitized master保存、JPG・PNGのorientation補正とJPEG再encode、EXIF・XMP・IPTC再検査、`photos`・`photo_assets`・upload結果の関連付けを実装した。Frontendはupload直後にsanitizeを呼び、成功・失敗・retryとHEICのJPEG変換案内を表示する。C14 merge commit `140abbb`はVercelとCloud Runへ自動配備され、Cloud Run最新revision `places-plates-api-00022-pw5`へtraffic 100%が切り替わった。偽GPS・camera XMPを含む合成JPGの本番uploadは新しい非公開旅行先draftへ遷移し、sanitize job `COMPLETED`、非公開`SANITIZED_MASTER`、`metadata_scan_passed=TRUE`を確認した。この過程で写真状態が`PROCESSING`に残る欠落を発見し、成功時に`READY`へ遷移し、完了request再呼出し時に既存写真の状態も復元するよう補完した。補完commit `a39f2d8`はCloud Run `places-plates-api-00024-ltz`へ配備され、traffic 100%を受ける。既存写真はUI再呼出し経路がなく`PROCESSING`に残ったため、完了job・非公開sanitized master・metadata検査成功の条件をすべて満たす既存行だけを`READY`へ変更するV11 backfillを追加した。本番provisioningでV11を1件適用し、対象写真を`READY`へ変更した。
- 重要ファイル: `backend/build.gradle.kts`、`backend/src/main/resources/application.yml`、`backend/src/main/resources/db/migration/common/V9__create_jdbc_session_store.sql`、`backend/src/main/resources/db/migration/postgresql/V10__secure_jdbc_session_store.sql`、`backend/src/main/resources/db/migration/common/V11__backfill_ready_sanitized_photos.sql`、`backend/src/main/java/com/placesplates/domain/auth/service/AdministratorPrincipal.java`、`backend/src/main/java/com/placesplates/domain/photo/service/ImageSanitizationService.java`、`backend/src/main/java/com/placesplates/infra/image/JavaImageSanitizer.java`、`backend/src/main/java/com/placesplates/infra/storage/SupabasePrivatePhotoStorage.java`、`backend/src/main/java/com/placesplates/infra/persistence/SupabaseDatabaseProvisioner.java`、`frontend/src/domain/photo/components/photo-uploader.tsx`、`backend/src/test/java/com/placesplates/infra/image/JavaImageSanitizerTests.java`、`backend/src/test/java/com/placesplates/infra/persistence/DatabaseMigrationTests.java`、`backend/src/test/java/com/placesplates/infra/persistence/JdbcSessionPersistenceTests.java`、`docs/AUTHENTICATION.md`、`docs/SUPABASE_DATABASE.md`、`docs/DEPLOYMENT_CHECKLIST.md`。
- 検証: Merge済み`main`のVerify・Secret protectionと`scripts/verify-all.ps1`が成功した。Secret検査、frontend lint・型検査・Vercel/Sites build、両公開成果物検査、backend全test、配備JAR生成が成功した。Backend testはJDBC session保存・復元、Cookieベースのlogin・logout、DB migration、provisioning契約を確認する。本番再実行で`PASS: Supabase database provisioned; 0 migration(s) applied in this run.`を確認し、Flyway v10とruntime role検証を完了した。C14 merge後にGitHub Verify・Secret protection、merge済み`main`の全local検証、Vercel HTTP 200、Cloud Run health API HTTP 200と`{"status":"UP"}`を再確認した。Cloud Run consoleでは`places-plates-api-00022-pw5`が最新revisionとしてtraffic 100%を受けることも確認した。新login後の管理画面reloadでも`管理者セッション有効`状態を復元した。本番合成JPGは53,508 byteからsanitized JPEG 54,674 byte・800×500へ再encodeされ、UUID専用storage keyと機密metadata検査成功を確認した。READY補完merge commitのGitHub checkと全local検証、Vercel配備、Cloud Run `places-plates-api-00024-ltz` traffic 100%、health API `{"status":"UP"}`を確認した。V11 testは完了・非公開・検査成功写真だけをREADYへ変更し、検査未通過写真はPROCESSINGに維持することを確認する。本番実行で`PASS: Supabase database provisioned; 1 migration(s) applied in this run.`を確認し、Supabase `flyway_schema_history`のV11、対象写真`READY`、条件付きbackfill残件0を確認した。Cloud Run `places-plates-api-00025-g2t` traffic 100%、Vercel HTTP 200、health API HTTP 200と`{"status":"UP"}`も再確認した。既存のNext.js `metadataBase`警告のみ残る。
- 決定: Sessionテーブルはユーザー所有データではなくserver認証基盤のためRLSを使わず、制限付きruntime roleのCRUDと公開role権限除去で保護する。本番では`spring.session.jdbc.initialize-schema=never`を維持し、schema変更をFlywayだけに任せる。Image sanitizeはCloud Runの常駐background CPUに依存せず認証済みupload flowのrequest単位で実行し、job row lockで重複処理を防ぐ。Sanitized masterは2,500万pixel上限、品質0.92 JPEG、非公開`sanitized/` UUID keyを使用する。
- リスク: 初回のruntime接続検証はSession pooler認証情報の伝播遅延で失敗したが、限定retry適用後に最終`PASS`を確認した。現在のJVMはHEIC・HEIF pixel decodeを対応しないため安全な失敗とJPEG変換案内を提供する。本番smokeで作成したtest draftは非公開で残っている。V11 backfillは適用済みであり、自動down migrationは提供しない。Application rollback pointは直前revision `places-plates-api-00024-ltz`である。
- 次の作業: 新しい合成JPGで新規READY遷移とsession持続性を再検証し、C15 responsive image variant・thumbnail生成を開始する。

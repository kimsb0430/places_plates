# 2026-08-31 Daily Report

## 한국어

- 목표: C41 운영·백업·비용·장애 대응 런북을 작성하고 다른 Supabase 환경으로의 복구 경계를 고정한다.
- 완료: RPO 24시간·RTO 4시간 초기 목표, DB와 비공개 Storage 분리 백업, 세션·임시 원본 제외, 새 Supabase 프로젝트 복원, Vercel·Cloud Run 롤백, P1~P3 장애와 비용 이상 대응 절차를 문서화했다. 핵심 복구 계약을 루트 검증에 추가했다.
- 중요 파일: `docs/OPERATIONS_RUNBOOK.md`, `scripts/check-operations-runbook.ps1`, `scripts/verify-all.ps1`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `docs/DEPLOYMENT_CHECKLIST.md`.
- 검증: 운영 런북 계약, 비밀정보 검사, 프론트 단위 테스트 22건, 시스템 Edge 데스크톱·모바일 E2E 2건, Next.js·Vinext 운영 빌드, 공개 산출물 검사와 Spring Boot 전체 테스트를 통과했다.
- 결정: Supabase DB 백업에는 Storage 객체가 없으므로 `sanitized/`·`variants/`를 같은 시각에 별도 보관한다. `temporary/` 원본과 로그인 세션은 복원하지 않는다. 월 1회 새 프로젝트 복원 훈련을 수행한다.
- 위험: 문서화만으로 실제 복구 가능성이 보장되지는 않는다. C42 전후에 첫 복원 훈련을 실행하고 파일 수·RLS·로그인·공개 사진을 증거로 남겨야 한다.
- 다음 작업: C41 PR 검증과 병합 후 C42 운영 도메인·예산·사용량 알림을 설정한다.
- C42 목표: 공급자 관리 운영 도메인과 무료·저비용 플랜에 맞는 예산·사용량 가드레일을 재현 가능한 계약으로 고정한다.
- C42 완료: Vercel·Cloud Run canonical URL, 기존 월 JPY 500 Google Cloud 예산의 실제 50%·80%·100%와 예측 100% 경고, Cloud Run 최소 0·최대 1 인스턴스, Dynamic Maps 월 9,000·Places Text Search 월 1,000 목표와 공급자 할당량 제약, Supabase Free·Vercel Hobby 주간 사용량 검토 기준을 정의했다.
- C42 중요 파일: `config/production-guardrails.json`, `docs/PRODUCTION_GUARDRAILS.md`, `scripts/check-production-guardrails.ps1`, `backend/cloudbuild.yaml`, `scripts/verify-cloud-run-container.ps1`, `scripts/verify-all.ps1`, `docs/DEPLOYMENT_CHECKLIST.md`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`.
- C42 검증: 운영 가드레일 JSON 계약과 Cloud Run 0~1 인스턴스 배포 계약을 통과했다. Google Cloud Console에서 기존 `Places Plates` 월 JPY 500 예산을 실제 50%·80%·100%와 예측 100% 경고로 저장하고 Cloud Run 서비스 자동 확장을 최소 0·최대 1로 적용했다. GitHub 운영 URL 변수 두 개를 등록했으며 Supabase Free의 Database·Storage·Egress·MAU가 제공량 아래임을 확인했다. Maps JavaScript 분당 30,000 한도는 조정 불가, Places Text Search 일 75,000·분당 600 한도는 무료 평가판에서 편집 불가임을 확인했다. Vercel은 Hobby 플랜이고 최근 사용량이 포함 한도 아래이며 Web·Email 알림이 모두 켜져 있음을 확인했다. 비밀정보 검사, 프런트 단위 테스트 22건, 시스템 Edge 데스크톱·모바일 E2E 2건, Next.js·Vinext 운영 빌드, 공개 산출물 검사와 Spring Boot 전체 테스트를 통과했다.
- C42 결정: v1은 추가 구매가 없는 공급자 관리 HTTPS 도메인을 운영 주소로 사용한다. Google Cloud 예산은 알림으로만 사용하고 결제를 자동 해제하지 않으며, Supabase Free와 Vercel Hobby의 유료 플랜 전환은 별도 승인을 요구한다.
- C42 위험: Google Cloud 예산은 지출 상한이 아니며 Maps·Places 공급자 할당량을 현재 계정에서 월 목표 수준으로 낮출 수 없다. 지연 로딩·관리자 전용 검색·월 누계 검토가 계속 필요하다.
- 다음 작업: C42 PR 병합 뒤 Production smoke를 확인하고 C43 v1 출시 점검을 진행한다.

## 日本語

- 目標: C41 operations・backup・cost・incident runbookを作成し、別Supabase環境へのrestore境界を固定する。
- 完了: RPO 24時間・RTO 4時間の初期目標、DBとprivate Storageの分離backup、session・temporary original除外、新Supabase project restore、Vercel・Cloud Run rollback、P1〜P3 incidentとcost異常対応を文書化した。主要recovery contractをroot verificationへ追加した。
- 重要ファイル: `docs/OPERATIONS_RUNBOOK.md`、`scripts/check-operations-runbook.ps1`、`scripts/verify-all.ps1`、`PROJECT_STRUCTURE.md`、`DEVELOPMENT_SCHEDULE.md`、`PLACES_AND_PLATES_PRODUCT_DESIGN.html`、`docs/DEPLOYMENT_CHECKLIST.md`。
- 検証: Operations runbook contract、secret scan、frontend unit test 22件、system Edge desktop・mobile E2E 2件、Next.js・Vinext production build、public artifact scan、Spring Boot全testに成功した。
- 決定: Supabase DB backupにStorage objectは含まれないため、`sanitized/`・`variants/`を同時刻に別保存する。`temporary/` originalとlogin sessionはrestoreしない。月1回、新project restore drillを行う。
- リスク: 文書化だけでは実restore可能性を保証できない。C42前後に初回restore drillを実行し、file数・RLS・login・public photoを証拠として残す必要がある。
- 次の作業: C41 PR verification・merge後、C42 production domain・budget・usage alertを設定する。
- C42目標: Provider管理production domainとfree・low-cost plan向けbudget・usage guardrailを再現可能なcontractとして固定する。
- C42完了: Vercel・Cloud Run canonical URL、既存の月JPY 500 Google Cloud budgetの実績50%・80%・100%と予測100% alert、Cloud Run min 0・max 1 instance、Dynamic Maps月9,000・Places Text Search月1,000目標とprovider quota制約、Supabase Free・Vercel Hobby週次usage review基準を定義した。
- C42重要ファイル: `config/production-guardrails.json`、`docs/PRODUCTION_GUARDRAILS.md`、`scripts/check-production-guardrails.ps1`、`backend/cloudbuild.yaml`、`scripts/verify-cloud-run-container.ps1`、`scripts/verify-all.ps1`、`docs/DEPLOYMENT_CHECKLIST.md`、`PROJECT_STRUCTURE.md`、`DEVELOPMENT_SCHEDULE.md`、`PLACES_AND_PLATES_PRODUCT_DESIGN.html`。
- C42検証: Production guardrails JSON contractとCloud Run 0〜1 instance deployment contractに成功した。Google Cloud Consoleで既存の`Places Plates`月JPY 500 budgetを実績50%・80%・100%と予測100% alertとして保存し、Cloud Run service autoscalingをmin 0・max 1へ適用した。GitHub production URL Variables 2件を登録し、Supabase FreeのDatabase・Storage・Egress・MAUがincluded limit未満であることを確認した。Maps JavaScriptの30,000回/分は変更不可、Places Text Searchの75,000回/日・600回/分はfree trialで編集不可と確認した。VercelはHobby planでrecent usageがincluded limit未満、Web・Email通知が両方onであることを確認した。Secret scan、frontend unit test 22件、system Edge desktop・mobile E2E 2件、Next.js・Vinext production build、public artifact scan、Spring Boot全testに成功した。
- C42決定: v1は追加購入のないprovider管理HTTPS domainをproduction addressとして使用する。Google Cloud budgetはalertだけに使いbillingを自動解除せず、Supabase FreeとVercel Hobbyのpaid plan移行は別approvalを必要とする。
- C42リスク: Google Cloud budgetは支出上限ではなく、Maps・Places provider quotaを現在のaccountで月間目標まで下げられない。Lazy load・admin限定search・月次累計reviewが引き続き必要である。
- 次の作業: C42 PR merge後にProduction smokeを確認し、C43 v1 release checkへ進む。

# 2026-08-30 Daily Report

## 한국어

- 목표: C38 핵심 E2E와 C39 운영 배포·배포 후 스모크를 완료하고, C40에서 실제 데이터 홈과 기록 삭제·상세 읽기 회귀를 수정한다.
- 완료: Playwright 설정과 격리형 API·TUS fixture로 업로드→게시→목록→지도를 연결했다. 이어서 운영 스모크를 추가했다. C40에서는 관리자 초안·게시 완료 기록 삭제와 Storage 정리, 실제 공개 API 기반 홈, 상단 여행 CTA, CATEGORY NOTE 상단 배치, 보호 공개 파생본 확대 화면을 구현했다.
- 중요 파일: `frontend/src/app/page.tsx`, `frontend/src/domain/post/components/public-photo-gallery.tsx`, `frontend/src/domain/post/components/published-post-list.tsx`, `PostManagementService.java`, `ManagedPostController.java`, `.github/workflows/production-smoke.yml`, `scripts/test-production-smoke.ps1`.
- 검증: C40 프론트 lint·타입 검사·단위 테스트 19건, 시스템 Edge 데스크톱·모바일 E2E 2건, Next.js 운영 빌드·지도 지연 로딩·공개 산출물·비밀정보 검사와 Spring Boot 전체 테스트를 통과했다.
- 결정: 기록 삭제는 복구 기능 없는 영구 삭제로 확인을 요구한다. Storage 정리가 실패하면 DB 삭제를 시작하지 않으며, 원본 미보관 정책에 따라 확대 화면에도 최대 2,000px의 정제·워터마크 공개본만 사용한다. 공유 가능성이 있는 장소 행은 삭제하지 않는다.
- 위험: Vercel·Cloud Build의 Git 연동은 검증과 병렬로 배포를 시작할 수 있다. 각 플랫폼의 원자적 전환으로 실패한 빌드는 기존 운영 버전을 유지하고, `Production smoke`는 성공한 `Verify` 뒤 실제 배포 버전을 확인한다.
- 다음 작업: C40 PR의 CI와 운영 배포 후 홈 실제 데이터, 초안·게시 기록 삭제, 사진 확대, 상세 정보 순서를 점검한다.

## 日本語

- 目標: C38 core E2EとC39 production deployment・post-deploy smokeを完了し、C40で実data home・record削除・detail閲覧のregressionを修正する。
- 完了: Playwright設定と隔離API・TUS fixtureでupload→publish→list→mapを接続し、production smokeを追加した。C40では管理者draft・published record削除とStorage cleanup、実public API home、上部travel CTA、CATEGORY NOTE上部配置、保護public derivative拡大画面を実装した。
- 重要ファイル: `frontend/src/app/page.tsx`、`frontend/src/domain/post/components/public-photo-gallery.tsx`、`frontend/src/domain/post/components/published-post-list.tsx`、`PostManagementService.java`、`ManagedPostController.java`、`.github/workflows/production-smoke.yml`、`scripts/test-production-smoke.ps1`。
- 検証: C40 frontend lint・type check・unit test 19件、system Edge desktop・mobile E2E 2件、Next.js production build・map lazy loading・public artifact・secret検査とSpring Boot全testに成功した。
- 決定: record削除は復元機能なしの永久削除として確認を要求する。Storage cleanup失敗時はDB削除を開始せず、original非保持方針に従って拡大画面にも最大2,000pxのsanitize・watermark済みpublic derivativeだけを使う。共有可能なplace行は保持する。
- リスク: Vercel・Cloud BuildのGit integrationはverificationと並行してdeploymentを開始できる。各platformのatomic切替により失敗buildは以前のproduction versionを維持し、`Production smoke`は成功した`Verify`後に実deployment versionを確認する。
- 次の作業: C40 PRのCIとproduction deployment後にhome実data、draft・published record削除、photo拡大、detail情報順序を確認する。

# 2026-08-23 Daily Report

## 한국어

- 목표: Places & Plates의 제품 방향, 예상 화면, 개발 일정과 저장소 운영 규칙을 확정한다.
- 완료: 맛집·여행지 목록과 지도 중심 목업, 이미지 보호 정책, Google Maps 방향, Next.js·Spring Boot 모노레포 구조를 문서화했다.
- 중요 파일: `frontend/`, `backend/`, `.github/workflows/verify.yml`, `.github/workflows/secret-protection.yml`, `scripts/check-secrets.ps1`, `docs/DEPLOYMENT_CHECKLIST.md`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `AGENTS.md`.
- 검증: 디자인 목업의 production build와 설계서 HTML 렌더링을 확인했다.
- 결정: 모든 작업은 `codex/*` 브랜치의 pull request로 제출하고 저장소 소유자가 Rebase and merge 한다.
- 위험: 실제 Google Maps와 이미지 처리 기능은 API 키와 운영 인프라가 준비된 뒤 통합해야 한다.
- Sprint 0 완료: 목업을 `frontend/src/app`으로 이전하고 도메인 구조를 생성했으며, Spring Boot 4.1.1·Java 21 백엔드와 `/api/v1/health`를 추가했다.
- Sprint 0 검증: 프론트엔드 lint·타입 검사·production build와 백엔드 테스트·bootJar 빌드를 통과했다.
- 안전 점검: 매일매일키워드 저장소를 참고해 커밋 후보 비밀정보 검사, 공개 빌드 산출물 검사, 배포 체크리스트를 추가했다.
- Vercel 수정: Sites용 Vinext 빌드와 Vercel용 표준 Next.js 빌드를 분리하고 `.next/routes-manifest.json` 검증을 추가했다.
- 병합 후 수정: 반복 검증에서 남아 있는 `.next` 타입과 충돌하지 않도록 `next typegen` 후 TypeScript 검사를 실행한다.
- Sprint 1 DB 준비: 소유자 중심 테이블, 맛집·여행지 전용 상세, 임시 업로드 원본 삭제 제약, PostGIS와 조회 인덱스를 Flyway 마이그레이션으로 구현했다.
- DB 검증: H2에 공통 마이그레이션을 재현하고 카테고리·공개 이미지·임시 원본 안전 제약 자동 테스트를 추가했다.
- 전체 검증: 프론트 lint·타입 검사·Vercel/Sites production build·공개 산출물 검사와 백엔드 test·bootJar가 통과했다.
- 결정: DB 관계와 인덱스를 UI보다 먼저 확정하고, 운영 PostgreSQL에는 PostGIS 전용 마이그레이션을 추가 적용한다.
- C05 완료: 색상·타이포·간격·반경·그림자·모션 토큰과 공통 헤더·본문·푸터 애플리케이션 셸을 추가했다.
- C05 반응형 검증: 390px·1440px 리스트와 지도 화면에서 문서 너비가 뷰포트와 일치하고 히어로 콜라주가 정상 렌더링됨을 실제 브라우저로 확인했다.
- C05 결정: 공통 UI는 의미 기반 토큰을 사용하고 최소 지원 너비를 320px로 유지한다.
- C06 완료: 홈·공개 기록·지도·로그인·404 경로와 현재 위치가 표시되는 공통 내비게이션, 데이터가 없을 때의 재사용 빈 상태를 추가했다.
- C06 반응형 검증: 390px·1440px의 5개 경로에서 HTTP 상태, 현재 메뉴, 로그인 비활성 입력, 가로 넘침을 실제 브라우저로 확인했다.
- C06 결정: 인증 연동 전 로그인 화면은 입력과 버튼을 비활성화하고 준비 상태를 명시해 동작하는 기능처럼 오해되지 않게 한다.
- 다음 작업: C06 PR 병합과 CI를 확인한 뒤 별도 브랜치에서 C08 관리자 인증 흐름을 시작한다.

## 日本語

- 目標: Places & Platesの製品方針、想定画面、開発日程、リポジトリ運用規則を確定する。
- 完了: グルメ・旅行先の一覧と地図中心モック、画像保護方針、Google Maps方針、Next.js・Spring Bootモノレポ構成を文書化した。
- 重要ファイル: `frontend/`, `backend/`, `.github/workflows/verify.yml`, `.github/workflows/secret-protection.yml`, `scripts/check-secrets.ps1`, `docs/DEPLOYMENT_CHECKLIST.md`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `AGENTS.md`。
- 検証: デザインモックのproduction buildと設計書HTMLのレンダリングを確認した。
- 決定: すべての作業は`codex/*`ブランチのpull requestとして提出し、リポジトリ所有者がRebase and mergeする。
- リスク: 実際のGoogle Mapsと画像処理機能はAPIキーと運用基盤の準備後に統合する必要がある。
- Sprint 0完了: モックを`frontend/src/app`へ移し、ドメイン構成を作成し、Spring Boot 4.1.1・Java 21バックエンドと`/api/v1/health`を追加した。
- Sprint 0検証: フロントエンドのlint・型検査・production buildとバックエンドのテスト・bootJarビルドに成功した。
- 安全検査: 毎日毎日キーワードのリポジトリを参考に、コミット候補のシークレット検査、公開ビルド成果物検査、デプロイチェックリストを追加した。
- Vercel修正: Sites用VinextビルドとVercel用標準Next.jsビルドを分離し、`.next/routes-manifest.json`検証を追加した。
- マージ後修正: 再検証時に残存する`.next`型との衝突を防ぐため、`next typegen`後にTypeScript検査を実行する。
- Sprint 1 DB準備: 所有者スコープのテーブル、グルメ・旅行先専用詳細、一時アップロード原本の削除制約、PostGISと検索インデックスをFlywayマイグレーションとして実装した。
- DB検証: H2で共通マイグレーションを再現し、カテゴリ・公開画像・一時原本の安全制約を確認する自動テストを追加した。
- 全体検証: フロントエンドのlint・型検査・Vercel/Sites production build・公開成果物検査とバックエンドのtest・bootJarに成功した。
- 決定: UIより先にDB関係とインデックスを確定し、運用PostgreSQLにはPostGIS専用マイグレーションを追加適用する。
- C05完了: 色・タイポグラフィ・間隔・角丸・影・モーションのトークンと、共通ヘッダー・本文・フッターのアプリケーションシェルを追加した。
- C05レスポンシブ検証: 390px・1440pxのリスト・地図画面で文書幅がビューポートと一致し、ヒーローコラージュが正常に表示されることを実ブラウザで確認した。
- C05決定: 共通UIは意味ベースのトークンを使用し、最小対応幅を320pxに維持する。
- C06完了: ホーム・公開記録・地図・ログイン・404ルートと現在位置を示す共通ナビゲーション、データ未登録時の再利用可能な空状態を追加した。
- C06レスポンシブ検証: 390px・1440pxの5ルートでHTTP状態、現在メニュー、無効化されたログイン入力、横方向のはみ出しを実ブラウザで確認した。
- C06決定: 認証接続前のログイン画面では入力とボタンを無効化し、準備状態を明記して利用可能な機能と誤認されないようにする。
- 次の作業: C06 PRのマージとCIを確認後、別ブランチでC08管理者認証フローを開始する。

# 2026-08-24 Daily Report

## 한국어

- 목표: 인증·RLS·Supabase·Cloud Run 기반을 운영에서 확인하고, C11 다중 사진 임시 업로드의 진행률·재시도·만료 추적을 구현한다.
- 완료: 12개 개인 데이터 테이블의 강제 RLS, 제한된 `placesplates_app` 역할, C10 보안 회귀, Google Buildpack Java 21 고정을 병합했다. Supabase 서울 리전 Supavisor 장애 복구 후 Cloud Run 실제 `/api/v1/health`가 `{"status":"UP"}`를 반환하는 것을 확인했다. C11에서 최대 100장 JPG·HEIC·PNG 선택, 6MB TUS 청크, 일시정지·재개, 사진별 진행률, 실패 항목 재시도, 24시간 만료, UUID 임시 객체 키, Storage 객체 크기 완료 검증을 추가했다. V6는 파일 표시명·MIME·크기·업로드 바이트·시도 횟수·실패 코드와 만료 조회 인덱스를 추가한다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/photo`, `backend/src/main/java/com/placesplates/infra/storage/SupabaseTemporaryUploadSigner.java`, `backend/src/main/resources/db/migration/common/V6__track_resumable_upload_progress.sql`, `backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`, `frontend/src/domain/photo`, `frontend/src/domain/auth/components/manage-session.tsx`, `frontend/src/app/globals.css`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`.
- 검증: 로컬 테스트 관리자와 H2로 로그인·관리 화면을 열어 업로드 영역의 데스크톱 레이아웃과 접근 가능한 파일 선택 상태를 시각 확인했다. `scripts/verify-all.ps1`에서 비밀 검사, 프론트 lint·타입 검사, Vercel Next.js 빌드, Sites Vinext 빌드, 두 공개 산출물 검사, 백엔드 test·bootJar가 모두 통과했다. Next.js의 기존 `metadataBase` 경고만 남았다.
- 결정: 인증·업로드 상태 제어는 Spring Boot API가 담당한다. 사진 본문만 백엔드가 소유자와 UUID 객체 키를 검증해 발급한 단기 서명 토큰으로 Supabase Storage TUS 엔드포인트에 직접 전송해 Cloud Run 프록시 비용과 요청 시간 제한을 피한다. 서비스 역할 키와 관리자 비밀번호는 Secret Manager에만 두며 Vercel이나 공개 산출물에 넣지 않는다.
- 위험: 운영 DB에는 병합 후 V6를 별도 적용해야 한다. 비공개 `temporary-uploads` 버킷과 Storage 서비스 역할 키를 Cloud Run에 설정하기 전에는 운영 업로드 API가 `PHOTO_STORAGE_UNAVAILABLE`을 반환한다. 실제 관리자 계정은 비밀번호를 Git에 넣지 않고 일회성 부트스트랩 리비전으로 생성해야 한다. C12~C17의 초안·이미지 정제·메타데이터 제거·워터마크·원본 삭제는 아직 구현 전이다.
- 다음 작업: C11 PR을 병합한 뒤 Supabase V6, 비공개 버킷, Cloud Run Storage 비밀을 적용하고 일회성 관리자 계정을 생성해 실제 사진 업로드 스모크 테스트를 수행한다. 이후 C12 업로드 시작 시 비공개 초안 생성을 구현한다.

## 日本語

- 目標: 認証・RLS・Supabase・Cloud Run基盤を本番で確認し、C11複数写真一時アップロードの進捗・再試行・有効期限追跡を実装する。
- 完了: 12個の個人データテーブルの強制RLS、制限付き`placesplates_app`ロール、C10セキュリティ回帰、Google Buildpack Java 21固定をマージした。SupabaseソウルリージョンのSupavisor障害復旧後、Cloud Runの実`/api/v1/health`が`{"status":"UP"}`を返すことを確認した。C11では最大100枚のJPG・HEIC・PNG、6MB TUSチャンク、一時停止・再開、写真別進捗、失敗項目の再試行、24時間有効期限、UUID一時オブジェクトキー、Storageオブジェクトサイズの完了検証を追加した。V6はファイル表示名・MIME・サイズ・アップロード済みバイト・試行回数・失敗コードと期限切れ検索インデックスを追加する。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/photo`、`backend/src/main/java/com/placesplates/infra/storage/SupabaseTemporaryUploadSigner.java`、`backend/src/main/resources/db/migration/common/V6__track_resumable_upload_progress.sql`、`backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`、`frontend/src/domain/photo`、`frontend/src/domain/auth/components/manage-session.tsx`、`frontend/src/app/globals.css`、`docs/SUPABASE_DATABASE.md`、`docs/DEPLOYMENT_CHECKLIST.md`。
- 検証: ローカルテスト管理者とH2でログインし、管理画面のアップロード領域についてデスクトップレイアウトとアクセシブルなファイル選択状態を目視確認した。`scripts/verify-all.ps1`で秘密検査、フロントエンドlint・型検査、Vercel Next.jsビルド、Sites Vinextビルド、両公開成果物検査、バックエンドtest・bootJarがすべて成功した。既存のNext.js `metadataBase`警告だけが残る。
- 決定: 認証とアップロード状態制御はSpring Boot APIが担当する。写真本文だけを、バックエンドが所有者とUUIDオブジェクトキーを検証して発行する短期署名トークンでSupabase Storage TUSへ直接送信し、Cloud Runプロキシ費用とリクエスト時間制限を回避する。サービスロールキーと管理者パスワードはSecret Managerだけへ保存し、Vercelや公開成果物には含めない。
- リスク: 本番DBにはマージ後にV6を別途適用する必要がある。非公開`temporary-uploads`バケットとStorageサービスロールキーをCloud Runへ設定するまでは、本番アップロードAPIが`PHOTO_STORAGE_UNAVAILABLE`を返す。実管理者はパスワードをGitへ入れず一回限りのブートストラップリビジョンで作成する。C12〜C17の下書き・画像サニタイズ・メタデータ削除・透かし・原本削除は未実装である。
- 次の作業: C11 PRマージ後にSupabase V6、非公開バケット、Cloud Run Storage秘密を適用し、一回限りの管理者を作成して実写真アップロードsmoke testを行う。その後C12のアップロード開始時非公開下書き作成を実装する。

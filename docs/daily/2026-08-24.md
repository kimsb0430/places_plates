# 2026-08-24 Daily Report

## 한국어

- 목표: 인증 사용자와 공개 방문자의 PostgreSQL 행 경계를 강제하고 Supabase 운영 데이터베이스를 안전하게 준비한다.
- 완료: 보호 API의 인증 UUID를 트랜잭션 로컬 DB 컨텍스트로 전달하고 12개 개인 데이터 테이블에 강제 RLS를 적용했다. Supabase 서울 리전 Free `nano` 프로젝트에 PostGIS 3.3.7과 Flyway V1~V5, 애플리케이션 테이블 13개를 적용했으며 제한된 `placesplates_app` 역할을 생성했다.
- 중요 파일: `backend/src/main/resources/db/migration/postgresql/V4__enforce_owner_scoped_row_security.sql`, `backend/src/main/resources/db/migration/postgresql/V5__grant_runtime_role_and_restrict_data_api.sql`, `backend/src/main/java/com/placesplates/infra/persistence/SupabaseDatabaseProvisioner.java`, `scripts/provision-supabase-database.ps1`, `.github/workflows/verify.yml`, `docs/SUPABASE_DATABASE.md`, `docs/DATABASE_SCHEMA.md`.
- 검증: H2 회귀·정책 구조·API 경계 테스트와 전체 프론트 빌드·백엔드 test·bootJar를 통과했다. PR CI의 PostGIS PostgreSQL 17에서 소유자·공개 모드 격리를 확인했고, 실제 Supabase에서 V1~V5 이력, PostGIS, 12개 강제 RLS 테이블, Data API 권한 차단, 제한된 런타임 역할 접속을 확인했다.
- 결정: 공개 API는 게시 완료·전체 공개 행과 안전 검사된 공개 사진만 읽는다. Supabase 관리자 계정은 일회성 마이그레이션에만 사용하고, 운영 Spring Boot는 `placesplates_app`, `FLYWAY_ENABLED=false`, 최대 DB 풀 5개로 시작한다. 프론트엔드는 Supabase에 직접 접근하지 않는다.
- 위험: Spring Boot 운영 호스팅이 아직 확정되지 않아 런타임 환경변수와 공개 API 스모크 테스트는 남아 있다. 관리자·런타임 비밀번호는 사용자만 보관하며 저장소와 로그에는 남기지 않았다. 백업·복원 절차는 실제 데이터 생성 전 별도 검증이 필요하다.
- 다음 작업: Supabase 운영 DB 준비 PR을 병합한 뒤 C10 인증·데이터 접근 정책 테스트를 확장한다.

## 日本語

- 目標: 認証ユーザーと公開訪問者のPostgreSQL行境界を強制し、Supabase本番データベースを安全に準備する。
- 完了: 保護APIの認証UUIDをトランザクションローカルDBコンテキストへ渡し、12個の個人データテーブルへ強制RLSを適用した。SupabaseソウルリージョンのFree `nano`プロジェクトへPostGIS 3.3.7、Flyway V1〜V5、アプリケーションテーブル13個を適用し、制限された`placesplates_app`ロールを作成した。
- 重要ファイル: `backend/src/main/resources/db/migration/postgresql/V4__enforce_owner_scoped_row_security.sql`、`backend/src/main/resources/db/migration/postgresql/V5__grant_runtime_role_and_restrict_data_api.sql`、`backend/src/main/java/com/placesplates/infra/persistence/SupabaseDatabaseProvisioner.java`、`scripts/provision-supabase-database.ps1`、`.github/workflows/verify.yml`、`docs/SUPABASE_DATABASE.md`、`docs/DATABASE_SCHEMA.md`。
- 検証: H2回帰・ポリシー構造・API境界テストとフロントエンド全ビルド・バックエンドtest・bootJarに成功した。PR CIのPostGIS PostgreSQL 17で所有者・公開モード分離を確認し、実SupabaseでV1〜V5履歴、PostGIS、12個の強制RLSテーブル、Data API権限遮断、制限された実行ロール接続を確認した。
- 決定: 公開APIは公開済み・全体公開の行と安全検査済み公開写真だけを読む。Supabase管理者は一時的なマイグレーションだけに使用し、本番Spring Bootは`placesplates_app`、`FLYWAY_ENABLED=false`、最大DBプール5から開始する。フロントエンドはSupabaseへ直接接続しない。
- リスク: Spring Boot本番ホスティングが未確定のため、実行環境変数と公開API smoke testは残っている。管理者・実行パスワードはユーザーだけが保管し、リポジトリとログには残していない。バックアップ・復元手順は実データ作成前に別途検証する必要がある。
- 次の作業: Supabase本番DB準備PRをマージ後、C10認証・データアクセス方針テストを拡張する。

# 2026-08-24 Daily Report

## 한국어

- 목표: 인증·RLS·Supabase·Cloud Run 기반과 C11 서명 TUS 업로드를 운영에서 확인하고, C12 비공개 초안과 C13 이미지 처리 재시도 큐까지 연결한다.
- 완료: 제한된 `placesplates_app` 역할과 강제 RLS, Google Buildpack Java 21, C11 최대 100장 TUS 임시 업로드를 병합·배포했다. `Invalid Compact JWS`는 서명 전용 `/upload/resumable/sign` 경로로 수정했다. C12는 맛집·여행지 선택 시 `PRIVATE`·`DRAFT` 게시물을 만들고 업로드 묶음과 연결하며, 완료 후 초안 화면 이동과 관리 화면 재진입을 제공한다. 실제 업로드 완료와 작성 중 초안 표시를 운영에서 확인했다. C13은 업로드 항목마다 UNIQUE 이미지 처리 작업을 생성하고 처리 시도 횟수, 최대 시도, 다음 실행 시각, 마지막 실패 원인을 추적하며 중복 완료 요청과 실패 작업 재시도를 지원한다. Supabase에 V7 작업 테이블·인덱스와 V8 강제 RLS·런타임 권한·Data API 차단을 적용하고, 커밋 `1d44af0` 이미지를 Cloud Run 버전 `places-plates-api-00018-k78`로 배포해 트래픽 100%를 전환했다. 폐기 가능한 `frontend/public/og.png` 한 장을 비공개 초안에 올려 업로드 항목과 `PENDING` 처리 작업이 각각 정확히 한 건 생성되는 것도 확인했다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/photo`, `backend/src/main/resources/db/migration/common/V7__create_image_processing_jobs.sql`, `backend/src/main/resources/db/migration/postgresql/V8__secure_image_processing_jobs.sql`, `backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`, `backend/src/test/java/com/placesplates/infra/persistence`, `frontend/src/domain/photo`, `frontend/src/domain/post`, `docs/DATABASE_SCHEMA.md`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`.
- 검증: 병합된 C13 `main`의 GitHub Verify·Secret protection과 로컬 전체 검증이 통과했다. `scripts/verify-all.ps1`의 비밀 검사, 프론트 lint·타입 검사·Vercel/Sites 빌드, 두 공개 산출물 검사, 백엔드 11개 테스트 suite와 배포 JAR 생성이 모두 성공했다. 운영 Vercel과 Cloud Run 상태 API는 각각 HTTP 200이고 상태 API는 `{"status":"UP"}`를 반환했다. 새 이미지 처리 작업 API의 비로그인 요청이 HTTP 401을 반환해 배포 및 인증 보호도 확인했다. 운영 초안 `1f337358-b083-4dcb-8c65-50b8592c8525`에서 작업 `0074fe39-1c4a-4b05-9bca-20e3d4d8d3ef` 한 건이 `PENDING`인 것을 확인했다. 기존 Next.js `metadataBase` 경고만 남아 있다.
- 결정: 인증·업로드 상태 제어와 이미지 처리 작업 큐는 Spring Boot가 담당한다. 사진 본문만 단기 서명 토큰으로 Supabase Storage에 직접 전송한다. 업로드 항목과 처리 작업은 1:1 UNIQUE 관계로 중복 처리를 막고, 실패 작업은 지수형 지연 시각과 최대 5회 시도를 기록한다. 운영에서는 `ADMIN_BOOTSTRAP_ENABLED=false`만 유지하고 `ADMIN_EMAIL`·`ADMIN_PASSWORD` Secret 참조를 Cloud Run 서비스에서 제거했다. Secret Manager의 비밀 자체는 삭제하지 않았다.
- 위험: V7·V8 적용 전 자동 생성된 버전 `places-plates-api-00017-ztm`은 스키마 검증으로 시작하지 못했지만 트래픽은 기존 정상 버전이 유지했다. 문서 병합으로 생성된 `places-plates-api-00019-fkl`은 정상이나 인메모리 로그인 세션이 초기화되어 첫 업로드가 인증 오류로 거절됐고, 다시 로그인한 뒤 성공했다. C13은 작업 생성·조회·재시도 계약까지이며 실제 이미지 디코딩, EXIF·XMP·IPTC 제거, 워터마크, 임시 원본 삭제는 C14~C17에서 구현한다. 따라서 개인정보가 있는 실제 사진은 공개하지 않고 폐기 가능한 테스트 이미지만 사용한다.
- 다음 작업: Cloud Run 리비전 교체에도 로그인이 유지되도록 Spring Session JDBC를 먼저 적용한 뒤 C14 정제 마스터와 메타데이터 제거를 구현한다.

## 日本語

- 目標: 認証・RLS・Supabase・Cloud Run基盤とC11署名付きTUSアップロードを本番で確認し、C12非公開下書きとC13画像処理再試行キューまで接続する。
- 完了: 制限付き`placesplates_app`ロールと強制RLS、Google Buildpack Java 21、C11最大100枚のTUS一時アップロードをマージ・配備した。`Invalid Compact JWS`は署名専用`/upload/resumable/sign`へ修正した。C12ではレストラン・旅行先選択時に`PRIVATE`・`DRAFT`投稿を作成してアップロードバッチと関連付け、完了後の下書き画面遷移と管理画面からの再入場を提供する。本番で実アップロード完了と作成中下書き表示を確認した。C13ではアップロード項目ごとにUNIQUE画像処理ジョブを作成し、試行回数、最大試行、次回実行時刻、最終失敗原因を追跡して重複完了要求と失敗ジョブ再試行を支援する。SupabaseへV7ジョブテーブル・indexとV8強制RLS・実行権限・Data API遮断を適用し、commit `1d44af0`のimageをCloud Run revision `places-plates-api-00018-k78`として配備してtraffic 100%を切り替えた。破棄可能な`frontend/public/og.png`を非公開draftへ1枚uploadし、upload itemと`PENDING`処理jobが各1件だけ生成されることも確認した。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/photo`、`backend/src/main/resources/db/migration/common/V7__create_image_processing_jobs.sql`、`backend/src/main/resources/db/migration/postgresql/V8__secure_image_processing_jobs.sql`、`backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`、`backend/src/test/java/com/placesplates/infra/persistence`、`frontend/src/domain/photo`、`frontend/src/domain/post`、`docs/DATABASE_SCHEMA.md`、`docs/SUPABASE_DATABASE.md`、`docs/DEPLOYMENT_CHECKLIST.md`。
- 検証: マージ済みC13 `main`のGitHub Verify・Secret protectionとローカル全検証が成功した。`scripts/verify-all.ps1`の秘密検査、フロントlint・型検査・Vercel/Sites build、両公開成果物検査、バックエンド11 test suite、配備JAR生成がすべて成功した。本番VercelとCloud Run health APIは各HTTP 200で、health APIは`{"status":"UP"}`を返した。新しい画像処理ジョブAPIへの未認証requestがHTTP 401を返し、配備と認証保護も確認した。本番draft `1f337358-b083-4dcb-8c65-50b8592c8525`でjob `0074fe39-1c4a-4b05-9bca-20e3d4d8d3ef`が1件だけ`PENDING`であることを確認した。既存のNext.js `metadataBase`警告のみ残る。
- 決定: 認証・アップロード状態制御と画像処理ジョブキューはSpring Bootが担当し、写真本文だけを短期署名トークンでSupabase Storageへ直接送信する。アップロード項目と処理ジョブは1対1 UNIQUE関係で重複処理を防ぎ、失敗ジョブは指数型遅延時刻と最大5回の試行を記録する。本番では`ADMIN_BOOTSTRAP_ENABLED=false`だけを維持し、`ADMIN_EMAIL`・`ADMIN_PASSWORD`のSecret参照をCloud Run serviceから削除した。Secret Manager内のsecret自体は削除していない。
- リスク: V7・V8適用前に自動生成されたrevision `places-plates-api-00017-ztm`はschema検証で起動できなかったが、trafficは既存の正常revisionに維持された。文書mergeで生成された`places-plates-api-00019-fkl`は正常だが、memory login sessionが初期化されて最初のuploadは認証errorとなり、再login後に成功した。C13はジョブ作成・照会・再試行契約までであり、実際の画像decode、EXIF・XMP・IPTC削除、透かし、一時原本削除はC14〜C17で実装する。個人情報を含む実写真は公開せず、破棄可能なテスト画像だけを使用する。
- 次の作業: Cloud Run revision交換後もloginを維持できるようSpring Session JDBCを先に適用し、その後C14サニタイズ済みmasterとmetadata削除を実装する。

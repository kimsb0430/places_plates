# 2026-08-24 Daily Report

## 한국어

- 목표: 인증·RLS·Supabase·Cloud Run 기반과 C11 서명 TUS 업로드를 운영에서 확인하고, C12 비공개 초안과 C13 이미지 처리 재시도 큐까지 연결한다.
- 완료: 제한된 `placesplates_app` 역할과 강제 RLS, Google Buildpack Java 21, C11 최대 100장 TUS 임시 업로드를 병합·배포했다. `Invalid Compact JWS`는 서명 전용 `/upload/resumable/sign` 경로로 수정했다. C12는 맛집·여행지 선택 시 `PRIVATE`·`DRAFT` 게시물을 만들고 업로드 묶음과 연결하며, 완료 후 초안 화면 이동과 관리 화면 재진입을 제공한다. 실제 업로드 완료와 작성 중 초안 표시를 운영에서 확인했다. C13은 업로드 항목마다 UNIQUE 이미지 처리 작업을 생성하고 처리 시도 횟수, 최대 시도, 다음 실행 시각, 마지막 실패 원인을 추적하며 중복 완료 요청과 실패 작업 재시도를 지원한다. V7은 작업 테이블과 인덱스, V8은 강제 RLS·런타임 권한·Supabase Data API 차단을 추가한다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/photo`, `backend/src/main/resources/db/migration/common/V7__create_image_processing_jobs.sql`, `backend/src/main/resources/db/migration/postgresql/V8__secure_image_processing_jobs.sql`, `backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`, `backend/src/test/java/com/placesplates/infra/persistence`, `frontend/src/domain/photo`, `frontend/src/domain/post`, `docs/DATABASE_SCHEMA.md`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`.
- 검증: 병합된 C12 `main`의 GitHub Verify·Secret protection, Vercel 운영 배포와 로컬 전체 검증이 통과했다. 운영에서 서명 TUS 업로드와 작성 중 초안 표시를 확인했다. C13 관련 Spring 컴파일, 업로드 완료·중복 방지·실패 재시도 API 통합 테스트, H2 마이그레이션 제약, PostgreSQL RLS·런타임 권한 정적 테스트가 통과했다. `scripts/verify-all.ps1`의 비밀 검사, 프론트 lint·타입 검사·Vercel/Sites 빌드, 두 공개 산출물 검사, 백엔드 11개 테스트 suite와 배포 JAR 생성도 모두 통과했다. 기존 Next.js `metadataBase` 경고만 남아 있다.
- 결정: 인증·업로드 상태 제어와 이미지 처리 작업 큐는 Spring Boot가 담당한다. 사진 본문만 단기 서명 토큰으로 Supabase Storage에 직접 전송한다. 업로드 항목과 처리 작업은 1:1 UNIQUE 관계로 중복 처리를 막고, 실패 작업은 지수형 지연 시각과 최대 5회 시도를 기록한다. 서비스 역할 키와 관리자 비밀번호는 Secret Manager에만 둔다.
- 위험: C13은 작업 생성·조회·재시도 계약까지이며 실제 이미지 디코딩, EXIF·XMP·IPTC 제거, 워터마크, 임시 원본 삭제는 C14~C17에서 구현한다. 따라서 개인정보가 있는 실제 사진은 공개하지 않고 폐기 가능한 테스트 이미지만 사용한다. C13 운영 배포 전에 관리자 연결로 Flyway V7·V8을 반드시 적용해야 한다.
- 다음 작업: 전체 검증과 PR 병합 후 Supabase V7·V8을 적용하고 Cloud Run을 배포해 업로드 완료 시 `image_processing_jobs`가 한 건 생성되는지 확인한다. 이후 C14 정제 마스터와 메타데이터 제거를 구현한다.

## 日本語

- 目標: 認証・RLS・Supabase・Cloud Run基盤とC11署名付きTUSアップロードを本番で確認し、C12非公開下書きとC13画像処理再試行キューまで接続する。
- 完了: 制限付き`placesplates_app`ロールと強制RLS、Google Buildpack Java 21、C11最大100枚のTUS一時アップロードをマージ・配備した。`Invalid Compact JWS`は署名専用`/upload/resumable/sign`へ修正した。C12ではレストラン・旅行先選択時に`PRIVATE`・`DRAFT`投稿を作成してアップロードバッチと関連付け、完了後の下書き画面遷移と管理画面からの再入場を提供する。本番で実アップロード完了と作成中下書き表示を確認した。C13ではアップロード項目ごとにUNIQUE画像処理ジョブを作成し、試行回数、最大試行、次回実行時刻、最終失敗原因を追跡して重複完了要求と失敗ジョブ再試行を支援する。V7はジョブテーブルとindex、V8は強制RLS・実行権限・Supabase Data API遮断を追加する。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/photo`、`backend/src/main/resources/db/migration/common/V7__create_image_processing_jobs.sql`、`backend/src/main/resources/db/migration/postgresql/V8__secure_image_processing_jobs.sql`、`backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`、`backend/src/test/java/com/placesplates/infra/persistence`、`frontend/src/domain/photo`、`frontend/src/domain/post`、`docs/DATABASE_SCHEMA.md`、`docs/SUPABASE_DATABASE.md`、`docs/DEPLOYMENT_CHECKLIST.md`。
- 検証: マージ済みC12 `main`のGitHub Verify・Secret protection、Vercel本番配備、ローカル全検証が成功した。本番で署名付きTUSアップロードと作成中下書き表示を確認した。C13関連のSpringコンパイル、アップロード完了・重複防止・失敗再試行API統合テスト、H2 migration制約、PostgreSQL RLS・実行権限静的テストが成功した。`scripts/verify-all.ps1`の秘密検査、フロントlint・型検査・Vercel/Sites build、両公開成果物検査、バックエンド11 test suiteと配備JAR生成もすべて成功した。既存のNext.js `metadataBase`警告のみ残る。
- 決定: 認証・アップロード状態制御と画像処理ジョブキューはSpring Bootが担当し、写真本文だけを短期署名トークンでSupabase Storageへ直接送信する。アップロード項目と処理ジョブは1対1 UNIQUE関係で重複処理を防ぎ、失敗ジョブは指数型遅延時刻と最大5回の試行を記録する。サービスロールキーと管理者パスワードはSecret Managerだけに保存する。
- リスク: C13はジョブ作成・照会・再試行契約までであり、実際の画像decode、EXIF・XMP・IPTC削除、透かし、一時原本削除はC14〜C17で実装する。個人情報を含む実写真は公開せず、破棄可能なテスト画像だけを使用する。C13本番配備前に管理者接続でFlyway V7・V8を必ず適用する。
- 次の作業: 全検証とPRマージ後にSupabase V7・V8を適用し、Cloud Runを配備してアップロード完了時に`image_processing_jobs`が1件作成されることを確認する。その後C14サニタイズ済みマスターとメタデータ削除を実装する。

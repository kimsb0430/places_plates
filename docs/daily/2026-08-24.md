# 2026-08-24 Daily Report

## 한국어

- 목표: 인증 사용자와 공개 방문자의 PostgreSQL 행 경계를 강제하고 Supabase 운영 데이터베이스를 준비한 뒤 C10 인증·데이터 접근 정책 회귀 테스트를 완성한다.
- 완료: 보호 API의 인증 UUID를 트랜잭션 로컬 DB 컨텍스트로 전달하고 12개 개인 데이터 테이블에 강제 RLS를 적용했다. Supabase 서울 리전 Free `nano` 프로젝트에 PostGIS 3.3.7과 Flyway V1~V5, 애플리케이션 테이블 13개, 제한된 `placesplates_app` 역할을 준비했다. 일반 회원·정지·탈퇴 계정, CSRF 실패 세션, 소유자 UUID 위조, 12개 RLS 테이블의 교차 계정 조회, 교차 수정·삭제·하위 사진 자산 연결, 공개 모드 쓰기를 자동 검증하도록 C10 테스트를 확장했다.
- 중요 파일: `backend/src/test/java/com/placesplates/api/AuthenticationControllerTests.java`, `backend/src/test/java/com/placesplates/global/security/DatabaseOwnerScopeFilterTests.java`, `backend/src/test/java/com/placesplates/infra/persistence/PostgresqlRowSecurityIntegrationTests.java`, `backend/src/main/resources/db/migration/postgresql/V4__enforce_owner_scoped_row_security.sql`, `backend/src/main/resources/db/migration/postgresql/V5__grant_runtime_role_and_restrict_data_api.sql`, `scripts/provision-supabase-database.ps1`, `docs/AUTHENTICATION.md`, `docs/SUPABASE_DATABASE.md`.
- 검증: 실제 Supabase 프로비저닝 검증과 기존 H2 회귀·정책 구조·API 경계 테스트를 통과했다. C10 인증 컨트롤러·DB 범위 필터 테스트와 전체 프론트 lint·타입 검사·두 배포 빌드·공개 산출물 검사, 백엔드 test·bootJar를 로컬에서 검증하며 PostgreSQL 전용 공격 시나리오는 PR CI의 PostGIS PostgreSQL 17에서 실행한다.
- 결정: 공개 API는 게시 완료·전체 공개 행과 안전 검사된 공개 사진만 읽는다. DB 소유자 범위는 요청 헤더가 아니라 인증된 `AdministratorPrincipal.userId`에서만 만들고, 로그인 중인 사용자가 공개 API를 호출해도 `PUBLIC` 모드를 유지한다. 인증 계정 상태 3종과 RLS 보호 테이블 12개를 보안 회귀 범위로 고정한다.
- 위험: Spring Boot 운영 호스팅이 아직 확정되지 않아 런타임 환경변수와 실제 공개 API 스모크 테스트는 남아 있다. 공개 게시물 API가 구현되면 정확한 방문일, 비공개 저장 키, 원본 파일명과 이미지 메타데이터가 DTO에 포함되지 않는지 계약 테스트를 추가해야 한다.
- 다음 작업: C10 pull request를 병합한 뒤 C11 다중 사진 임시 업로드와 만료 추적을 시작한다.

## 日本語

- 目標: 認証ユーザーと公開訪問者のPostgreSQL行境界を強制し、Supabase本番データベースを準備した後、C10認証・データアクセス方針の回帰テストを完成させる。
- 完了: 保護APIの認証UUIDをトランザクションローカルDBコンテキストへ渡し、12個の個人データテーブルへ強制RLSを適用した。SupabaseソウルリージョンのFree `nano`プロジェクトへPostGIS 3.3.7、Flyway V1〜V5、アプリケーションテーブル13個、制限された`placesplates_app`ロールを準備した。一般会員・停止・退会アカウント、CSRF失敗時のセッション、所有者UUID偽装、12個のRLSテーブルにおける他アカウント参照、他アカウントの更新・削除・子写真アセット関連付け、公開モードの書き込みを自動検証するようC10テストを拡張した。
- 重要ファイル: `backend/src/test/java/com/placesplates/api/AuthenticationControllerTests.java`、`backend/src/test/java/com/placesplates/global/security/DatabaseOwnerScopeFilterTests.java`、`backend/src/test/java/com/placesplates/infra/persistence/PostgresqlRowSecurityIntegrationTests.java`、`backend/src/main/resources/db/migration/postgresql/V4__enforce_owner_scoped_row_security.sql`、`backend/src/main/resources/db/migration/postgresql/V5__grant_runtime_role_and_restrict_data_api.sql`、`scripts/provision-supabase-database.ps1`、`docs/AUTHENTICATION.md`、`docs/SUPABASE_DATABASE.md`。
- 検証: 実Supabaseのプロビジョニング検証と既存H2回帰・方針構造・API境界テストに成功した。C10認証コントローラー・DBスコープフィルターテスト、フロントエンドlint・型検査・2種類の配備ビルド・公開成果物検査、バックエンドtest・bootJarをローカルで検証し、PostgreSQL専用の攻撃シナリオはPR CIのPostGIS PostgreSQL 17で実行する。
- 決定: 公開APIは公開済み・全体公開の行と安全検査済み公開写真だけを読む。DB所有者スコープはリクエストヘッダーではなく認証済み`AdministratorPrincipal.userId`だけから作成し、ログイン中に公開APIを呼び出しても`PUBLIC`モードを維持する。認証アカウント状態3種類とRLS保護テーブル12個をセキュリティ回帰範囲として固定する。
- リスク: Spring Boot本番ホスティングが未確定のため、実行環境変数と実公開API smoke testは残っている。公開投稿APIの実装時に、正確な訪問日、非公開ストレージキー、元ファイル名、画像メタデータがDTOに含まれないことを契約テストへ追加する必要がある。
- 次の作業: C10 pull requestをマージした後、C11複数写真の一時アップロードと有効期限追跡を開始する。

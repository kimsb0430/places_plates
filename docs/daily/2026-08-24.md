# 2026-08-24 Daily Report

## 한국어

- 목표: 인증·RLS·Supabase·Cloud Run 기반을 운영에서 확인하고, C11 다중 사진 임시 업로드의 진행률·재시도·만료 추적을 구현한다.
- 완료: 12개 개인 데이터 테이블의 강제 RLS, 제한된 `placesplates_app` 역할, C10 보안 회귀, Google Buildpack Java 21 고정을 병합했다. Supabase 서울 리전 Supavisor 장애 복구 후 Cloud Run 실제 `/api/v1/health`가 `{"status":"UP"}`를 반환하는 것을 확인했다. C11에서 최대 100장 JPG·HEIC·PNG 선택, 6MB TUS 청크, 일시정지·재개, 사진별 진행률, 실패 항목 재시도, 24시간 만료, UUID 임시 객체 키, Storage 객체 크기 완료 검증을 추가했다. V6는 파일 표시명·MIME·크기·업로드 바이트·시도 횟수·실패 코드와 만료 조회 인덱스를 추가한다. 운영 업로드에서 발견된 `Invalid Compact JWS` 원인을 일반 TUS 경로 사용으로 확인하고 서명 전용 `/upload/resumable/sign`으로 수정했다. 병합 커밋 `c15dfa1`의 Cloud Build `8103e6e3`가 성공했고 Cloud Run `places-plates-api-00013-ppx`가 100% 트래픽으로 배포됐다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/photo`, `backend/src/main/java/com/placesplates/infra/storage/SupabaseTemporaryUploadSigner.java`, `backend/src/main/resources/db/migration/common/V6__track_resumable_upload_progress.sql`, `backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`, `backend/src/test/java/com/placesplates/infra/storage/SupabaseTemporaryUploadSignerTests.java`, `frontend/src/domain/photo`, `frontend/src/domain/auth/components/manage-session.tsx`, `frontend/src/app/globals.css`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`.
- 검증: 로컬 테스트 관리자와 H2로 로그인·관리 화면을 열어 업로드 영역의 데스크톱 레이아웃과 접근 가능한 파일 선택 상태를 시각 확인했다. 서명 발급 회귀 테스트와 업로드 API 통합 테스트가 통과했다. `scripts/verify-all.ps1`에서 비밀 검사, 프론트 lint·타입 검사, Vercel Next.js 빌드, Sites Vinext 빌드, 두 공개 산출물 검사, 백엔드 test·bootJar가 모두 통과했다. 최초 전체 검증에서 Windows Next.js 워커가 한 번 비정상 종료했지만 단독 빌드와 전체 재실행에서는 재현되지 않았다. 병합된 `main`의 GitHub Verify·Secret protection과 재실행한 전체 로컬 검증도 통과했다. 운영 `https://places-plates-api-481849639838.asia-northeast3.run.app/api/v1/health`는 HTTP 200과 `{"status":"UP"}`를 반환했다. Next.js의 기존 `metadataBase` 경고만 남았다.
- 결정: 인증·업로드 상태 제어는 Spring Boot API가 담당한다. 사진 본문만 백엔드가 소유자와 UUID 객체 키를 검증해 발급한 단기 서명 토큰으로 Supabase Storage TUS 엔드포인트에 직접 전송해 Cloud Run 프록시 비용과 요청 시간 제한을 피한다. 서비스 역할 키와 관리자 비밀번호는 Secret Manager에만 두며 Vercel이나 공개 산출물에 넣지 않는다.
- 위험: 브라우저에서 서명 TUS 업로드가 실제로 완료되는지는 배포 후 폐기 가능한 테스트 이미지로 한 번 더 확인해야 한다. C12~C17의 초안·이미지 정제·메타데이터 제거·워터마크·원본 삭제는 아직 구현 전이므로 개인정보가 포함된 실제 사진은 사용하지 않는다. 배포 롤백 지점은 직전 Revision `places-plates-api-00012-24d`와 커밋 `7215d7e`다.
- 다음 작업: 폐기 가능한 테스트 이미지로 `/storage/v1/upload/resumable/sign` 생성 요청과 업로드 완료를 확인한다. 성공하면 C12 업로드 시작 시 비공개 초안 생성을 구현한다.

## 日本語

- 目標: 認証・RLS・Supabase・Cloud Run基盤を本番で確認し、C11複数写真一時アップロードの進捗・再試行・有効期限追跡を実装する。
- 完了: 12個の個人データテーブルの強制RLS、制限付き`placesplates_app`ロール、C10セキュリティ回帰、Google Buildpack Java 21固定をマージした。SupabaseソウルリージョンのSupavisor障害復旧後、Cloud Runの実`/api/v1/health`が`{"status":"UP"}`を返すことを確認した。C11では最大100枚のJPG・HEIC・PNG、6MB TUSチャンク、一時停止・再開、写真別進捗、失敗項目の再試行、24時間有効期限、UUID一時オブジェクトキー、Storageオブジェクトサイズの完了検証を追加した。V6はファイル表示名・MIME・サイズ・アップロード済みバイト・試行回数・失敗コードと期限切れ検索インデックスを追加する。本番アップロードで判明した`Invalid Compact JWS`の原因を通常TUSパスの使用と特定し、署名専用`/upload/resumable/sign`へ修正した。マージコミット`c15dfa1`のCloud Build `8103e6e3`が成功し、Cloud Run `places-plates-api-00013-ppx`をトラフィック100%で配備した。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/photo`、`backend/src/main/java/com/placesplates/infra/storage/SupabaseTemporaryUploadSigner.java`、`backend/src/main/resources/db/migration/common/V6__track_resumable_upload_progress.sql`、`backend/src/test/java/com/placesplates/api/PhotoUploadControllerTests.java`、`backend/src/test/java/com/placesplates/infra/storage/SupabaseTemporaryUploadSignerTests.java`、`frontend/src/domain/photo`、`frontend/src/domain/auth/components/manage-session.tsx`、`frontend/src/app/globals.css`、`docs/SUPABASE_DATABASE.md`、`docs/DEPLOYMENT_CHECKLIST.md`。
- 検証: ローカルテスト管理者とH2でログインし、管理画面のアップロード領域についてデスクトップレイアウトとアクセシブルなファイル選択状態を目視確認した。署名発行の回帰テストとアップロードAPI統合テストが成功した。`scripts/verify-all.ps1`で秘密検査、フロントエンドlint・型検査、Vercel Next.jsビルド、Sites Vinextビルド、両公開成果物検査、バックエンドtest・bootJarがすべて成功した。最初の全体検証ではWindowsのNext.jsワーカーが一度異常終了したが、単独ビルドと全体再実行では再現しなかった。マージ後の`main`でGitHub Verify・Secret protectionとローカル全体検証も成功した。本番`https://places-plates-api-481849639838.asia-northeast3.run.app/api/v1/health`はHTTP 200と`{"status":"UP"}`を返した。既存のNext.js `metadataBase`警告だけが残る。
- 決定: 認証とアップロード状態制御はSpring Boot APIが担当する。写真本文だけを、バックエンドが所有者とUUIDオブジェクトキーを検証して発行する短期署名トークンでSupabase Storage TUSへ直接送信し、Cloud Runプロキシ費用とリクエスト時間制限を回避する。サービスロールキーと管理者パスワードはSecret Managerだけへ保存し、Vercelや公開成果物には含めない。
- リスク: ブラウザの署名付きTUSアップロードが実際に完了することは、配備後に破棄可能なテスト画像で再確認する必要がある。C12〜C17の下書き・画像サニタイズ・メタデータ削除・透かし・原本削除は未実装のため、個人情報を含む実写真は使用しない。配備のロールバック地点は直前Revision `places-plates-api-00012-24d`とコミット`7215d7e`である。
- 次の作業: 破棄可能なテスト画像で`/storage/v1/upload/resumable/sign`の作成リクエストとアップロード完了を確認する。成功後、C12のアップロード開始時非公開下書き作成を実装する。

# 2026-08-26 Daily Report

## 한국어

- 목표: Cloud Run에서 ICC 색상 프로필이 있는 JPEG 정제 시 발생한 LCMS2 네이티브 라이브러리 오류를 제거한다.
- 완료: Google Buildpack 설정을 Java 21 다단계 Dockerfile로 교체하고 실행 이미지에 `liblcms2-2`를 설치했다. 애플리케이션은 UID 10001 비루트 사용자로 실행하며 CI가 실제 이미지를 빌드해 `liblcms2.so.2`와 실행 사용자를 검사하도록 추가했다. 병합 후 기존 Cloud Build 트리거가 Dockerfile을 무시하고 인라인 Buildpacks를 실행하는 문제를 확인해, 저장소의 `backend/cloudbuild.yaml`이 Docker 이미지 빌드·푸시·Cloud Run 서비스 갱신을 담당하도록 후속 보완했다. 운영 트리거를 저장소 구성 파일로 전환하고 병합 커밋 `a852fca`를 수동 실행해 새 리비전 배포까지 완료했다. 실제 개인 사진 재시도는 정제·반응형 파생본 생성·초안 이동까지 성공했다. 이어서 C16에서 세 파생본 하단 오른쪽에 `Places & Plates`를 픽셀 합성하고, 배경 밝기 기반 색상 선택·정책 버전·위치 기록·기존 C15 파생본 재생성을 구현했다. 운영 DB에 V12를 적용한 뒤 PR #31을 병합하고 새 Cloud Run 리비전을 배포했으며, 공개 샘플 이미지로 비공개 초안을 만들어 운영 워터마크 정제 경로를 검증했다.
- C17 완료: 저장소에서 정제 마스터와 세 파생본을 다시 읽어 JPEG 디코딩·바이트 크기·해상도·EXIF/XMP/IPTC 0건을 확인하고 정제 마스터에서 동일 워터마크 정책으로 재생성한 바이트와 일치할 때만 파생본을 공개하도록 변경했다. 신규 파생본은 검증 전 비공개로 기록하며, 임시 원본 삭제까지 성공해야 업로드 항목을 `COMPLETED`, 사진을 `READY`로 전환한다. 삭제 실패는 원본 키와 비공개 처리 상태를 유지해 재시도하고, 검증 실패 원본과 24시간 만료 미처리 원본은 삭제 후 실패·만료 상태로 닫는다. 예약 정리는 제한 함수로 후보 소유자 UUID만 얻고 실제 항목을 소유자 RLS 범위에서 처리한다.
- C17 운영 배포: 운영 DB에 V13·V14를 적용해 `PASS: Supabase database provisioned; 2 migration(s) applied in this run.`을 확인했다. PR #33의 병합 커밋 `09edd36`을 기준으로 Cloud Build `166986dd-1462-47f0-8943-bf2c6f54c337`가 성공했으며, Cloud Run 리비전 `places-plates-api-00032-zrl`이 트래픽 100%를 받는다. Vercel과 Cloud Run health가 HTTP 200이고 API는 `{"status":"UP"}`를 반환한다.
- C18 완료: 비공개 초안에 제목·방문 월·한줄평·본문을 조회·수정하는 소유자 범위 `PATCH` API를 추가했다. 방문 월은 공개 정책에 맞춰 연도·월 쌍으로 저장하고, 제목은 필수지만 나머지 항목이 비어 있는 부분 초안도 저장할 수 있다. Next.js 편집 화면은 필수 3개 항목의 진행도를 보여주고 입력 중단 700ms 후 CSRF 보호 자동 저장을 실행하며 저장 중·완료·실패·재시도 상태를 제공한다. 장소 연결은 C21로 유지한다.
- 중요 파일: `backend/Dockerfile`, `backend/cloudbuild.yaml`, `backend/src/main/java/com/placesplates/infra/image/JavaServerWatermarkRenderer.java`, `backend/src/main/java/com/placesplates/infra/image/JavaStoredImageVerifier.java`, `backend/src/main/java/com/placesplates/domain/photo/service/ImageSanitizationService.java`, `backend/src/main/java/com/placesplates/domain/photo/service/PhotoAssetVerificationService.java`, `backend/src/main/java/com/placesplates/domain/photo/service/TemporaryOriginalCleanupService.java`, `backend/src/main/resources/db/migration/common/V13__enforce_temporary_original_purge.sql`, `backend/src/main/resources/db/migration/postgresql/V14__list_temporary_original_cleanup_owners.sql`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- C18 중요 파일: `backend/src/main/java/com/placesplates/domain/post/`, `backend/src/main/java/com/placesplates/global/security/SecurityConfig.java`, `backend/src/test/java/com/placesplates/api/DraftPostControllerTests.java`, `frontend/src/domain/post/`, `frontend/src/app/globals.css`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`
- 검증: 병합 전후 비밀 검사, 컨테이너 계약 검사, 프론트 lint·타입 검사·Vercel 및 Sites 빌드와 공개 산출물 검사, 백엔드 전체 테스트·배포 JAR 생성이 통과했다. GitHub CI의 실제 Docker 빌드와 `liblcms2.so.2`·비루트 UID 검사도 통과했다. 병합 커밋 `9bace43`의 첫 Cloud Build는 인라인 Buildpacks가 Java 21을 찾지 못해 실패했지만 이전 리비전이 트래픽 100%를 유지해 운영 중단은 없었다. 저장소 구성으로 전환한 Cloud Build와 문서 병합 후 자동 빌드가 성공했다. C16 병합 커밋 `e273950`의 GitHub Actions와 Cloud Build도 성공했고 `places-plates-api-00030-c6k`가 트래픽 100%를 받는다. Vercel과 Cloud Run health는 HTTP 200이며 API는 `{"status":"UP"}`를 반환했다. 실제 사진의 `/complete`와 `/sanitize`는 HTTP 200, 사진은 `READY`, 초안 조회도 HTTP 200이었으며 최근 로그에 `UnsatisfiedLinkError`가 없었다. 공개 샘플 이미지 운영 테스트도 `/sanitize` HTTP 200으로 완료되어 `THUMBNAIL`·`MAP_CARD`·`PUBLIC_DETAIL` 생성 경로와 V12 워터마크 정책 제약을 통과했다. C16 백엔드 테스트는 어두운·밝은 배경의 픽셀 변화, 하단 오른쪽 위치, 메타데이터 0건, V12 제약, 신규·기존 파생본 정책 전환을 확인한다.
- C17 검증: 비밀 검사, Cloud Run 컨테이너 계약, 프론트 lint·타입 검사·Vercel/Sites 빌드와 두 공개 산출물 검사, 백엔드 전체 테스트·배포 JAR 생성이 통과했다. 자동 테스트는 저장 바이트 재검사, EXIF 거부, 워터마크 파생본 일치, 삭제 성공 후 상태 전환, 삭제 장애 후 비공개 유지와 재시도, 만료 원본 삭제, V13 제약, Storage GET·DELETE 계약을 확인한다.
- C17 운영 스모크: 공개 저장소 샘플 `frontend/public/og.png`를 맛집 사진으로 업로드해 비공개 초안 `41fb0130-c24d-4fc1-ae80-4bd249e84b13`의 편집 화면까지 정상 이동했다. DB 읽기 전용 조회에서 업로드 항목 `5a958b0c-6463-42f4-9449-d55f4b30cbc3`은 `COMPLETED`, 임시 저장 키는 `NULL`, 원본 삭제 시각은 `2026-08-26 04:59:36.948692+00`, 연결 사진 `64776786-aa49-4cae-867d-5290c165b2c3`은 `READY`였다. `SANITIZED_MASTER`는 비공개·메타데이터 검사 통과·워터마크 없음이고, `THUMBNAIL`·`MAP_CARD`·`PUBLIC_DETAIL`은 공개·메타데이터 검사 통과·`places-plates-corner-v1` 우하단 워터마크 적용 상태다.
- C18 검증: 비밀 검사, Cloud Run 컨테이너 계약, 프론트 lint·타입 검사·Vercel/Sites 빌드와 두 공개 산출물 검사, 백엔드 전체 테스트·배포 JAR 생성이 통과했다. 백엔드 대상 테스트는 저장 후 재조회, 선택 항목 초기화, 연도·월 쌍 검증, 다른 소유자 초안 은닉, 비인증 수정 차단을 확인했다.
- C18 운영 검증: PR #35의 병합 커밋 `aee1ec8`을 기준으로 GitHub Actions `Secret protection`과 `Verify`가 성공했다. Vercel 관리 화면과 Cloud Run health는 HTTP 200이고 API는 `{"status":"UP"}`를 반환했다. 기존 비공개 초안 `41fb0130-c24d-4fc1-ae80-4bd249e84b13`에서 제목·한줄평·본문을 임시 수정한 뒤 `자동 저장됨` 상태와 새로고침 후 DB 재조회를 확인했으며, 테스트 값은 원래 제목 `새 맛집 기록`과 빈 선택 항목으로 복구하고 다시 새로고침해 확인했다. C18은 기존 스키마를 사용하므로 추가 DB 마이그레이션이 없다. 애플리케이션 롤백 지점은 C17 정상 리비전 `places-plates-api-00032-zrl`이다.
- 결정: Cloud Run 백엔드는 암묵적인 Buildpack 시스템 패키지에 의존하지 않고 저장소의 Dockerfile을 실행 이미지 기준으로 사용한다. Cloud Build 트리거는 인라인 구성이 아니라 버전 관리되는 `backend/cloudbuild.yaml`을 사용한다.
- 위험: 공개 이미지 제공 API와 게시물 공개 기능은 아직 구현 전이므로 저장된 워터마크 파생본의 공개 페이지 시각 검증은 C23까지 남아 있다. C17 예약 정리는 Cloud Run 인스턴스가 0개일 때 실행되지 않아 정확한 시각 보장이 필요해지면 Cloud Scheduler 호출형 작업으로 분리해야 한다. 애플리케이션 롤백 지점은 C17 정상 리비전 `places-plates-api-00032-zrl`이며 V13·V14 DB 변경은 자동 롤백하지 않는다.
- 다음 작업: C19 맛집 전용 필드를 구현한다.

## 日本語

- 目標: Cloud RunでICC color profileを含むJPEGをsanitizeするときに発生するLCMS2 native library errorを解消する。
- 完了: Google Buildpack設定をJava 21のmulti-stage Dockerfileへ置き換え、runtime imageへ`liblcms2-2`を導入した。ApplicationはUID 10001の非root userで実行し、CIが実imageをbuildして`liblcms2.so.2`と実行userを検査するようにした。Merge後、既存Cloud Build triggerがDockerfileを無視してinline Buildpacksを実行する問題を確認し、repositoryの`backend/cloudbuild.yaml`がDocker imageのbuild・push・Cloud Run service更新を担当するように補完した。本番triggerをrepository構成fileへ切り替え、merge commit `a852fca`を手動実行して新revisionの配備まで完了した。実際の個人写真retryはsanitize・responsive variant生成・draft遷移まで成功した。続いてC16で3 variantの右下へ`Places & Plates`をpixel合成し、背景輝度による色選択・policy version・位置記録・既存C15 variant再生成を実装した。本番DBへV12を適用してPR #31をmergeし、新Cloud Run revisionを配備した後、公開sample画像から非公開draftを作成して本番watermark sanitize経路を検証した。
- C17完了: Storageからsanitized masterと3 variantを再読込し、JPEG decode・byte size・解像度・EXIF/XMP/IPTC 0件を確認する。Sanitized masterから同じwatermark policyで再生成したbytesと一致した場合だけvariantを公開するよう変更した。新規variantは検証前に非公開で記録し、一時原本削除まで成功した場合だけupload itemを`COMPLETED`、photoを`READY`へ変更する。削除失敗時は原本keyと非公開処理状態を保持して再試行し、検証失敗原本と24時間期限切れ未処理原本は削除後に失敗・期限切れ状態へ閉じる。定期cleanupは限定関数から候補owner UUIDだけを取得し、実itemをowner RLS範囲で処理する。
- C17本番配備: 本番DBへV13・V14を適用し、`PASS: Supabase database provisioned; 2 migration(s) applied in this run.`を確認した。PR #33のmerge commit `09edd36`を基準にCloud Build `166986dd-1462-47f0-8943-bf2c6f54c337`が成功し、Cloud Run revision `places-plates-api-00032-zrl`がtraffic 100%を受ける。VercelとCloud Run healthはHTTP 200で、APIは`{"status":"UP"}`を返す。
- C18完了: 非公開draftのtitle・visited month・summary・bodyを照会・更新するowner scoped `PATCH` APIを追加した。Visited monthは公開policyに合わせてyear・monthのpairで保存し、titleは必須だがその他のfieldが空のpartial draftも保存できる。Next.js編集画面は必須3項目の進捗を表示し、入力停止700ms後にCSRF保護autosaveを実行してsaving・saved・failed・retry状態を提供する。Place接続はC21の範囲に維持する。
- 重要ファイル: `backend/Dockerfile`、`backend/cloudbuild.yaml`、`backend/src/main/java/com/placesplates/infra/image/JavaServerWatermarkRenderer.java`、`backend/src/main/java/com/placesplates/infra/image/JavaStoredImageVerifier.java`、`backend/src/main/java/com/placesplates/domain/photo/service/ImageSanitizationService.java`、`backend/src/main/java/com/placesplates/domain/photo/service/PhotoAssetVerificationService.java`、`backend/src/main/java/com/placesplates/domain/photo/service/TemporaryOriginalCleanupService.java`、`backend/src/main/resources/db/migration/common/V13__enforce_temporary_original_purge.sql`、`backend/src/main/resources/db/migration/postgresql/V14__list_temporary_original_cleanup_owners.sql`、`PLACES_AND_PLATES_PRODUCT_DESIGN.html`、`PROJECT_STRUCTURE.md`、`DEVELOPMENT_SCHEDULE.md`、`docs/DEPLOYMENT_CHECKLIST.md`
- C18重要ファイル: `backend/src/main/java/com/placesplates/domain/post/`、`backend/src/main/java/com/placesplates/global/security/SecurityConfig.java`、`backend/src/test/java/com/placesplates/api/DraftPostControllerTests.java`、`frontend/src/domain/post/`、`frontend/src/app/globals.css`、`PLACES_AND_PLATES_PRODUCT_DESIGN.html`、`PROJECT_STRUCTURE.md`、`DEVELOPMENT_SCHEDULE.md`
- 検証: Merge前後のsecret検査、container contract検査、frontend lint・型検査・VercelおよびSites buildと公開成果物検査、backend全test・配備JAR生成が成功した。GitHub CIの実Docker buildと`liblcms2.so.2`・非root UID検査も成功した。Merge commit `9bace43`の初回Cloud Buildはinline BuildpacksがJava 21を見つけられず失敗したが、旧revisionがtraffic 100%を維持して本番停止はなかった。Repository構成への切替後とdocument merge後のCloud Buildは成功した。C16 merge commit `e273950`のGitHub ActionsとCloud Buildも成功し、`places-plates-api-00030-c6k`がtraffic 100%を受ける。VercelとCloud Run healthはHTTP 200で、APIは`{"status":"UP"}`を返した。実写真の`/complete`と`/sanitize`はHTTP 200、photoは`READY`、draft取得もHTTP 200で、最近のlogに`UnsatisfiedLinkError`はなかった。公開sample画像の本番testも`/sanitize` HTTP 200で完了し、`THUMBNAIL`・`MAP_CARD`・`PUBLIC_DETAIL`生成経路とV12 watermark policy制約を通過した。C16 backend testは暗い・明るい背景のpixel変化、右下位置、metadata 0件、V12制約、新規・既存variantのpolicy遷移を確認する。
- C17検証: Secret検査、Cloud Run container contract、frontend lint・type検査・Vercel/Sites buildと2つの公開成果物検査、backend全test・配備JAR生成が成功した。自動testは保存bytes再検査、EXIF拒否、watermark variant一致、削除成功後の状態遷移、削除障害後の非公開維持と再試行、期限切れ原本削除、V13制約、Storage GET・DELETE契約を確認する。
- C17本番smoke: 公開repository sample `frontend/public/og.png`をレストラン写真としてuploadし、非公開draft `41fb0130-c24d-4fc1-ae80-4bd249e84b13`の編集画面まで正常遷移した。DBのread-only照会でupload item `5a958b0c-6463-42f4-9449-d55f4b30cbc3`は`COMPLETED`、一時storage keyは`NULL`、原本削除時刻は`2026-08-26 04:59:36.948692+00`、接続photo `64776786-aa49-4cae-867d-5290c165b2c3`は`READY`だった。`SANITIZED_MASTER`は非公開・metadata検査通過・watermarkなし、`THUMBNAIL`・`MAP_CARD`・`PUBLIC_DETAIL`は公開・metadata検査通過・`places-plates-corner-v1`右下watermark適用状態である。
- C18検証: Secret検査、Cloud Run container contract、frontend lint・type検査・Vercel/Sites buildと2つの公開成果物検査、backend全test・配備JAR生成が成功した。Backend対象testは保存後の再読込、optional field clear、year・month pair検証、他owner draftの隠蔽、未認証update拒否を確認した。
- C18本番検証: PR #35のmerge commit `aee1ec8`を基準にGitHub Actionsの`Secret protection`と`Verify`が成功した。Vercel管理画面とCloud Run healthはHTTP 200で、APIは`{"status":"UP"}`を返した。既存の非公開draft `41fb0130-c24d-4fc1-ae80-4bd249e84b13`でtitle・summary・bodyを一時更新し、`自動保存完了`状態とrefresh後のDB再読込を確認した。Test値は元のtitle `새 맛집 기록`と空のoptional fieldへ戻し、再度refreshして復元を確認した。C18は既存schemaを利用するため、追加DB migrationはない。Application rollback地点はC17の正常revision `places-plates-api-00032-zrl`である。
- 決定: Cloud Run backendは暗黙的なBuildpack system packageへ依存せず、repositoryのDockerfileをruntime image基準として使用する。Cloud Build triggerはinline設定ではなくversion管理された`backend/cloudbuild.yaml`を使用する。
- リスク: 公開画像提供APIとpost公開機能は未実装のため、保存済みwatermark variantの公開page視覚確認はC23まで残る。C17定期cleanupはCloud Run instanceが0個の間は実行されないため、正確な時刻保証が必要になればCloud Scheduler呼出し型jobへ分離する。Application rollback地点はC17の正常revision `places-plates-api-00032-zrl`であり、V13・V14のDB変更は自動rollbackしない。
- 次の作業: C19 restaurant専用fieldを実装する。

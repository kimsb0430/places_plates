# 2026-08-26 Daily Report

## 한국어

- 목표: Cloud Run에서 ICC 색상 프로필이 있는 JPEG 정제 시 발생한 LCMS2 네이티브 라이브러리 오류를 제거한다.
- 완료: Google Buildpack 설정을 Java 21 다단계 Dockerfile로 교체하고 실행 이미지에 `liblcms2-2`를 설치했다. 애플리케이션은 UID 10001 비루트 사용자로 실행하며 CI가 실제 이미지를 빌드해 `liblcms2.so.2`와 실행 사용자를 검사하도록 추가했다. 병합 후 기존 Cloud Build 트리거가 Dockerfile을 무시하고 인라인 Buildpacks를 실행하는 문제를 확인해, 저장소의 `backend/cloudbuild.yaml`이 Docker 이미지 빌드·푸시·Cloud Run 서비스 갱신을 담당하도록 후속 보완했다. 운영 트리거를 저장소 구성 파일로 전환하고 병합 커밋 `a852fca`를 수동 실행해 새 리비전 배포까지 완료했다. 실제 개인 사진 재시도는 정제·반응형 파생본 생성·초안 이동까지 성공했다. 이어서 C16에서 세 파생본 하단 오른쪽에 `Places & Plates`를 픽셀 합성하고, 배경 밝기 기반 색상 선택·정책 버전·위치 기록·기존 C15 파생본 재생성을 구현했다.
- 중요 파일: `backend/Dockerfile`, `backend/cloudbuild.yaml`, `backend/src/main/java/com/placesplates/infra/image/JavaServerWatermarkRenderer.java`, `backend/src/main/java/com/placesplates/domain/photo/entity/PhotoAsset.java`, `backend/src/main/java/com/placesplates/domain/photo/service/ImageSanitizationService.java`, `backend/src/main/resources/db/migration/common/V12__record_server_watermark_policy.sql`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 검증: 병합 전후 비밀 검사, 컨테이너 계약 검사, 프론트 lint·타입 검사·Vercel 및 Sites 빌드와 공개 산출물 검사, 백엔드 전체 테스트·배포 JAR 생성이 통과했다. GitHub CI의 실제 Docker 빌드와 `liblcms2.so.2`·비루트 UID 검사도 통과했다. 병합 커밋 `9bace43`의 첫 Cloud Build는 인라인 Buildpacks가 Java 21을 찾지 못해 실패했지만 이전 리비전이 트래픽 100%를 유지해 운영 중단은 없었다. 저장소 구성으로 전환한 Cloud Build와 문서 병합 후 자동 빌드가 성공했고 최신 리비전 `places-plates-api-00029-j4r`가 트래픽 100%를 받는다. 실제 사진의 `/complete`와 `/sanitize`는 HTTP 200, 사진은 `READY`, 초안 조회도 HTTP 200이었으며 최근 로그에 `UnsatisfiedLinkError`가 없었다. C16 백엔드 테스트는 어두운·밝은 배경의 픽셀 변화, 하단 오른쪽 위치, 메타데이터 0건, V12 제약, 신규·기존 파생본 정책 전환을 확인한다.
- 결정: Cloud Run 백엔드는 암묵적인 Buildpack 시스템 패키지에 의존하지 않고 저장소의 Dockerfile을 실행 이미지 기준으로 사용한다. Cloud Build 트리거는 인라인 구성이 아니라 버전 관리되는 `backend/cloudbuild.yaml`을 사용한다.
- 위험: C16 배포 전 운영 DB에 V12를 적용해야 하며, 배포 후 실제 파생본 픽셀과 정책 필드를 운영에서 확인하기 전까지 워터마크 기능의 운영 검증은 완료되지 않는다. 애플리케이션 롤백 지점은 `places-plates-api-00029-j4r`다.
- 다음 작업: C16 PR 병합 후 V12를 프로비저닝하고 Cloud Run 배포·워터마크 운영 스모크를 완료한 뒤 C17 공개본 검사와 임시 원본 삭제를 진행한다.

## 日本語

- 目標: Cloud RunでICC color profileを含むJPEGをsanitizeするときに発生するLCMS2 native library errorを解消する。
- 完了: Google Buildpack設定をJava 21のmulti-stage Dockerfileへ置き換え、runtime imageへ`liblcms2-2`を導入した。ApplicationはUID 10001の非root userで実行し、CIが実imageをbuildして`liblcms2.so.2`と実行userを検査するようにした。Merge後、既存Cloud Build triggerがDockerfileを無視してinline Buildpacksを実行する問題を確認し、repositoryの`backend/cloudbuild.yaml`がDocker imageのbuild・push・Cloud Run service更新を担当するように補完した。本番triggerをrepository構成fileへ切り替え、merge commit `a852fca`を手動実行して新revisionの配備まで完了した。実際の個人写真retryはsanitize・responsive variant生成・draft遷移まで成功した。続いてC16で3 variantの右下へ`Places & Plates`をpixel合成し、背景輝度による色選択・policy version・位置記録・既存C15 variant再生成を実装した。
- 重要ファイル: `backend/Dockerfile`、`backend/cloudbuild.yaml`、`backend/src/main/java/com/placesplates/infra/image/JavaServerWatermarkRenderer.java`、`backend/src/main/java/com/placesplates/domain/photo/entity/PhotoAsset.java`、`backend/src/main/java/com/placesplates/domain/photo/service/ImageSanitizationService.java`、`backend/src/main/resources/db/migration/common/V12__record_server_watermark_policy.sql`、`PLACES_AND_PLATES_PRODUCT_DESIGN.html`、`PROJECT_STRUCTURE.md`、`DEVELOPMENT_SCHEDULE.md`、`docs/DEPLOYMENT_CHECKLIST.md`
- 検証: Merge前後のsecret検査、container contract検査、frontend lint・型検査・VercelおよびSites buildと公開成果物検査、backend全test・配備JAR生成が成功した。GitHub CIの実Docker buildと`liblcms2.so.2`・非root UID検査も成功した。Merge commit `9bace43`の初回Cloud Buildはinline BuildpacksがJava 21を見つけられず失敗したが、旧revisionがtraffic 100%を維持して本番停止はなかった。Repository構成への切替後とdocument merge後のCloud Buildは成功し、最新revision `places-plates-api-00029-j4r`がtraffic 100%を受ける。実写真の`/complete`と`/sanitize`はHTTP 200、photoは`READY`、draft取得もHTTP 200で、最近のlogに`UnsatisfiedLinkError`はなかった。C16 backend testは暗い・明るい背景のpixel変化、右下位置、metadata 0件、V12制約、新規・既存variantのpolicy遷移を確認する。
- 決定: Cloud Run backendは暗黙的なBuildpack system packageへ依存せず、repositoryのDockerfileをruntime image基準として使用する。Cloud Build triggerはinline設定ではなくversion管理された`backend/cloudbuild.yaml`を使用する。
- リスク: C16配備前に本番DBへV12を適用する必要があり、配備後に実variant pixelとpolicy fieldを本番確認するまでwatermarkの本番検証は未完了である。Application rollback地点は`places-plates-api-00029-j4r`である。
- 次の作業: C16 PR merge後にV12をprovisioningし、Cloud Run配備・watermark本番smokeを完了してからC17公開画像検査と一時原本削除へ進む。

# 2026-08-28 Daily Report

## 한국어

- 목표: C25~C29 공개 탐색 흐름을 병합·운영 검증하고, Sprint 5 C30 인접 지도 마커 묶음과 숫자를 구현한다.
- 완료: PR #43 병합 커밋 `610f442`의 GitHub 검사와 Vercel·Cloud Run 상태를 확인하고 최신 `main` 전체 검증을 통과했다. C25는 공개 목록에 안정적인 정렬과 워터마크 대표 사진 카드를 추가했고, C25A는 공개 오류 응답을 보완했다. C26은 `/posts/{postId}`에서 공통 본문·월 단위 방문 시기·장소·카테고리 전용 정보와 검증된 `PUBLIC_DETAIL`만 제공한다. PR #46 병합 커밋 `23473c2`의 main CI, Vercel 배포, Cloud Run health와 새 상세 API 계약까지 확인했다. C27은 내부 장소 ID 대신 공개 게시물 ID로 진입하는 `/posts/{postId}/place`와 장소 이력 API를 추가해 같은 소유자·장소의 전체 공개 방문 수·월별 타임라인·검증된 `MAP_CARD`·각 상세 링크를 제공한다. V15는 공개 게시물에 연결된 장소만 PUBLIC RLS로 읽게 하며 다른 회원 기록·좌표·내부 ID·비공개·링크 공개 기록은 제외한다. 운영 V15를 적용하고 PR #47 병합 커밋 `8ac770a`의 main CI, Vercel, Cloud Run health와 새 장소 이력 API 오류 계약까지 확인했다. C28은 전체·카테고리 목록, 상세·대표 사진·상세 사진·장소 이력의 모든 공개 읽기 경로와 PostgreSQL 게시물·카테고리 상세·장소·사진·자산에 `PUBLIC + PUBLISHED` 경계를 회귀 테스트로 고정한다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/post/service/PublicPostService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/PublicPostController.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPlaceHistoryResponse.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPlaceVisitResponse.java`, `backend/src/main/resources/db/migration/postgresql/V15__allow_public_linked_place_read.sql`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `backend/src/test/java/com/placesplates/infra/persistence/PostgresqlRowSecurityIntegrationTests.java`, `frontend/src/app/posts/[postId]/place/page.tsx`, `frontend/src/domain/post/components/public-place-history.tsx`, `frontend/src/domain/post/components/public-post-detail.tsx`, `frontend/src/domain/post/api/public-post-api.ts`, `frontend/src/app/globals.css`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 검증: 병합된 `main`에서 비밀정보·컨테이너 계약·프론트 Vercel/Sites 빌드·공개 산출물·백엔드 전체 검증을 통과했다. C26 테스트는 상세 분리·공개 범위·안전한 사진을 확인했다. C27 대상 테스트는 같은 소유자·장소 방문의 월별 정렬과 합계, 안전한 대표 사진, 비공개·링크 공개 차단, V15 정책 문자열을 확인했다. 운영 V15에서 `PASS: Supabase database provisioned; 1 migration(s) applied in this run.`을 확인했고 PR #47 병합 후 동일한 전체 검증과 GitHub Backend·Frontend·Secret protection, Vercel HTTP 200, Cloud Run `{"status":"UP"}`, 새 장소 이력 API의 `PUBLIC_PLACE_HISTORY_NOT_FOUND` 응답을 확인했다. C28 대상 테스트는 `PRIVATE`, `UNLISTED`, `DRAFT`가 전역·카테고리 합계와 목록에서 빠지고 상세·대표 사진·상세 사진·장소 이력의 직접 조회가 모두 404인지 확인한다. PostgreSQL CI에서는 `PUBLIC + DRAFT`, `UNLISTED + PUBLISHED`, `PRIVATE + PUBLISHED`에 연결된 장소·사진·자산까지 RLS에서 제외한다.
- 결정: 카드와 상세 이미지 경로는 저장소 URL이 아닌 백엔드 공개 API 상대 경로만 반환한다. 장소 페이지 URL은 내부 장소 ID가 아니라 이미 공개된 게시물 ID를 진입점으로 사용한다. 서버만 `place_id`를 해석하고 공개 방문 DTO에는 장소명·Google 지도 링크·연월만 제공한다. 공개 범위 회귀는 서비스 쿼리와 PostgreSQL RLS의 이중 경계를 같은 상태 행렬로 검증한다. 브라우저 보호 레이어는 보조 수단이며 원본 미제공·EXIF 제거·픽셀 워터마크를 실제 보호 경계로 유지한다.
- 위험: 공개 이미지 요청은 현재 Cloud Run을 거쳐 비공개 Supabase Storage를 읽으므로 트래픽 증가 시 캐시 비용을 재검토해야 한다. 링크 공개 공유 주소는 아직 없으며 C28은 링크 공개 게시물을 공개 목록에서 차단하는 현재 계약만 검증한다. C28은 테스트·문서 작업이므로 DB 마이그레이션과 환경변수 변경이 없다.
- C29 완료: 비로그인 `GET /api/v1/map/posts`와 `/map` 서버·클라이언트 경계를 추가했다. 전체·맛집·여행지 URL 필터는 API 결과·전역 합계와 일치하며 맛집은 주황색 ‘맛’, 여행지는 초록색 ‘여’ Google 지도 마커로 표시한다. 사용자가 버튼을 누를 때만 공식 JS API loader가 지도를 불러오고, Map ID가 있으면 Advanced Marker를 사용한다. 지도 정보창은 문자열 HTML 삽입 없이 DOM `textContent`로 조립해 공개 상세로 연결한다. 지도 API는 `PUBLIC + PUBLISHED`, 좌표 허용, 위도·경도 쌍과 Google 좌표 30일 유효성을 검사하며 `APPROXIMATE` 좌표를 소수점 둘째 자리로 낮춘다. V16은 좌표가 연결된 기존 게시물의 `coordinate_visibility`을 `EXACT`로 보정한다.
- C29 검증: `scripts/verify-all.ps1`에서 비밀정보·Cloud Run 컨테이너 계약, 프론트 lint·타입 검사·Vercel/Sites 빌드·두 공개 산출물 검사, 전체 백엔드 테스트와 배포 JAR 생성을 통과했다. 지도 테스트는 공개 범위·카테고리 합계와 필터·정확/근사 좌표·30일 만료 Google 좌표·잘못된 쿼리, 보안 필터의 PUBLIC DB 모드와 V16 조건부 보정을 확인한다.
- C29 결정: Google 장소 선택 또는 직접 위도·경도 입력은 지도 표시 의사로 간주해 좌표 연결 시 `EXACT`, 좌표가 없으면 `HIDDEN`으로 저장한다. 비공개·링크 공개 게시물은 좌표 상태와 관계없이 공개 지도에서 제외하고, 30일이 지난 Google 좌표는 자동 노출하지 않는다. 인접 마커 묶음과 영역 합계는 C30·C31로 분리한다.
- C29 위험: 운영 배포 전에 Supabase V16 적용과 Vercel `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` 설정이 필요하다. 키는 비밀 서버 키가 아니라 브라우저 공개 키이므로 HTTP 리퍼러와 Maps JavaScript API로 제한해야 한다. Advanced Marker용 Map ID가 없으면 기본 마커 호환 경로를 사용한다.
- C29 운영 확인: Supabase V16 적용, 브라우저 전용 `places-plates-browser-maps` 키와 JavaScript Vector Map ID 연결 후 운영 `/map`에서 전체 공개 게시물과 Google 지도가 표시되는 것을 확인했다. 최초 `ApiTargetBlockedMapError`는 Vercel에 다른 키가 입력된 것이 원인이었고 브라우저 전용 키로 교체·재배포해 해결했다.
- C30 완료: 공식 `@googlemaps/markerclusterer` 2.6.2와 Supercluster를 공개 지도에 연결했다. 인접 게시물은 확대 수준에 따라 반경 72px 안에서 묶고 숫자에 실제 포함 게시물 수를 표시하며, 묶음 선택 시 기본 범위 확대 동작으로 개별 마커를 확인한다. 맛집 전용 묶음은 주황색, 여행지 전용 묶음은 초록색, 혼합 묶음은 브랜드색으로 표시하고 Advanced Marker·기본 Marker 양쪽을 지원한다.
- C30 검증: 프론트엔드 lint와 Next.js 타입 검사를 먼저 통과했다. 최종 전체 검증에서는 Vercel/Sites 빌드, 공개 산출물 비밀정보 검사와 기존 백엔드 회귀 테스트를 함께 실행한다.
- C30 결정: 클러스터 숫자는 서버가 별도 집계하지 않고 이미 공개 범위와 카테고리 필터를 통과한 현재 API 결과를 공식 클라이언트 알고리즘이 묶은 실제 마커 수로 계산한다. 현재 화면 경계의 전체 합계는 클러스터 수와 의미가 다르므로 C31에서 지도 idle 이벤트 기준으로 분리한다.
- C30 위험: 운영에는 현재 지도 게시물이 한 개뿐이라 실제 숫자 묶음 스모크는 인접 공개 게시물이 두 개 이상일 때 확인해야 한다. 지도 로드는 기존처럼 명시적 버튼 뒤에만 발생하므로 과금 경계는 바뀌지 않는다.
- 다음 작업: C30 PR 병합과 인접 게시물 운영 스모크를 확인한 뒤 C31 현재 지도 화면 경계 게시물 합계를 구현한다.

## 日本語

- 目標: C25〜C29 public explore flowをmerge・production検証し、Sprint 5 C30近接map marker clusterと数値を実装する。
- 完了: PR #43 merge commit `610f442`のGitHub検査とVercel・Cloud Run状態を確認し、最新`main`の全体検証を通過した。C25は安定sortとwatermark cover card、C25Aはpublic error responseを修正した。C26は`/posts/{postId}`で共通本文・月単位visit時期・place・category専用情報と検証済み`PUBLIC_DETAIL`だけを提供する。PR #46 merge commit `23473c2`のmain CI、Vercel配備、Cloud Run health、新detail API契約まで確認した。C27はinternal place IDの代わりにpublic post IDで進入する`/posts/{postId}/place`とplace history APIを追加し、同一owner・placeのpublic visit数・月別timeline・検証済み`MAP_CARD`・各detail linkを提供する。V15はpublic postに接続されたplaceだけをPUBLIC RLSで読めるようにし、他member記録・coordinate・internal ID・private・unlisted記録を除外する。Production V15を適用し、PR #47 merge commit `8ac770a`のmain CI、Vercel、Cloud Run health、新place history API error契約を確認した。C28はall・category list、detail・cover・detail photo・place historyの全public read pathとPostgreSQLのpost・category detail・place・photo・assetへ`PUBLIC + PUBLISHED`境界をregression testとして固定する。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/post/service/PublicPostService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/PublicPostController.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPlaceHistoryResponse.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPlaceVisitResponse.java`, `backend/src/main/resources/db/migration/postgresql/V15__allow_public_linked_place_read.sql`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `backend/src/test/java/com/placesplates/infra/persistence/PostgresqlRowSecurityIntegrationTests.java`, `frontend/src/app/posts/[postId]/place/page.tsx`, `frontend/src/domain/post/components/public-place-history.tsx`, `frontend/src/domain/post/components/public-post-detail.tsx`, `frontend/src/domain/post/api/public-post-api.ts`, `frontend/src/app/globals.css`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`, `docs/SUPABASE_DATABASE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 検証: merge済み`main`でsecret・container contract・frontend Vercel/Sites build・public artifact・backend全体検証を通過した。C26 testはdetail分離・公開範囲・安全なphotoを確認した。C27対象testは同一owner・place visitの月別sortとcount、安全なcover、private・unlisted遮断、V15 policy文字列を確認した。Production V15で`PASS: Supabase database provisioned; 1 migration(s) applied in this run.`を確認し、PR #47 merge後に同じ全体検証、GitHub Backend・Frontend・Secret protection、Vercel HTTP 200、Cloud Run `{"status":"UP"}`、新place history APIの`PUBLIC_PLACE_HISTORY_NOT_FOUND`を確認した。C28対象testは`PRIVATE`・`UNLISTED`・`DRAFT`がglobal/category countとlistから除外され、detail・cover・detail photo・place historyのdirect readがすべて404になることを確認する。PostgreSQL CIでは`PUBLIC + DRAFT`、`UNLISTED + PUBLISHED`、`PRIVATE + PUBLISHED`に接続されたplace・photo・assetもRLSから除外する。
- 決定: Cardとdetail image pathはStorage URLではなくbackend public API relative pathだけを返す。Place page URLはinternal place IDではなく既にpublicなpost IDをentry pointにする。Serverだけが`place_id`を解釈し、public visit DTOにはplace名・Google Maps link・年月だけを提供する。Public scope regressionはservice queryとPostgreSQL RLSの二重境界を同じstate matrixで検証する。Browser保護layerは補助手段とし、original非提供・EXIF削除・pixel watermarkを実際の保護境界として維持する。
- リスク: Public image requestはCloud Run経由でprivate Supabase Storageを読むためtraffic増加時はcache費用を再検討する。Unlisted共有URLはまだなく、C28はunlisted postをpublic listから遮断する現在契約だけを検証する。C28はtest・document作業のためDB migrationとenvironment variable変更はない。
- C29完了: 未login `GET /api/v1/map/posts`と`/map` server・client境界を追加した。All・restaurant・destination URL filterはAPI result・global countと一致し、restaurantはorange「맛」、destinationはgreen「여」のGoogle map markerで表示する。Userがbuttonを押したときだけofficial JS API loaderがmapを読み、Map IDがあればAdvanced Markerを使う。Info windowはHTML string挿入を避けDOM `textContent`で組み立てpublic detailへ接続する。Map APIは`PUBLIC + PUBLISHED`、coordinate許可、緯度・経度pair、Google coordinate 30日有効性を検査し、`APPROXIMATE`は小数第2位へ下げる。V16はcoordinate接続済みの既存postを`coordinate_visibility=EXACT`へ補正する。
- C29検証: `scripts/verify-all.ps1`でsecret・Cloud Run container contract、frontend lint・type check・Vercel/Sites build・両public artifact検査、backend全testとdeploy JAR生成を通過した。Map testはpublic scope・category count/filter・exact/approximate coordinate・30日expired Google coordinate・invalid query、security filterのPUBLIC DB modeとV16 conditional backfillを確認する。
- C29決定: Google place選択または緯度・経度直接入力をmap表示意図として扱い、coordinate接続時は`EXACT`、coordinateなしは`HIDDEN`で保存する。Private・unlisted postはcoordinate状態に関係なくpublic mapから除外し、30日を過ぎたGoogle coordinateは自動公開しない。Nearby marker clusterとbounds countはC30・C31へ分離する。
- C29リスク: Production配備前にSupabase V16適用とVercel `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`設定が必要である。Keyはserver secretではなくbrowser public keyなのでHTTP referrerとMaps JavaScript APIで制限する。Advanced Marker用Map IDがなければdefault marker compatibility pathを使う。
- C29本番確認: Supabase V16、browser専用`places-plates-browser-maps` key、JavaScript Vector Map IDを接続し、production `/map`でpublic postとGoogle mapの表示を確認した。最初の`ApiTargetBlockedMapError`はVercelへ別keyを入力したことが原因で、browser専用keyへ交換・再配備して解決した。
- C30完了: 公式`@googlemaps/markerclusterer` 2.6.2とSuperclusterをpublic mapへ接続した。近接投稿はzoom levelに応じて72px半径内でcluster化し、数値は実際に含む投稿数を表示する。cluster選択ではdefault bounds zoomで個別markerを確認する。Restaurant専用clusterはorange、destination専用はgreen、mixed clusterはbrand colorを使い、Advanced Marker・default Markerの両方を支援する。
- C30検証: frontend lintとNext.js type checkを先に通過した。最終全体検証ではVercel/Sites build、public artifact secret scan、既存backend regression testを実行する。
- C30決定: Cluster数はserverで別集計せず、public scopeとcategory filterを通過した現在API結果を公式client algorithmがまとめた実marker数を使う。Current bounds合計はcluster数と意味が異なるためC31でmap idle event基準として分離する。
- C30リスク: Production map postは現在1件だけなので、実際のcluster number smokeは近接public postが2件以上になった時に確認する。Map loadは従来どおり明示button後だけで、billing boundaryは変わらない。
- 次の作業: C30 PR mergeと近接投稿production smoke確認後、C31 current map bounds post countを実装する。

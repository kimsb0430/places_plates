# 2026-08-28 Daily Report

## 한국어

- 목표: 병합된 C24 운영 상태를 검증하고 C25 공개 대표 사진 카드와 C25A 오류 응답 보완을 전달한 뒤, C26 카테고리별 공개 상세 페이지를 구현한다.
- 완료: PR #43 병합 커밋 `610f442`의 GitHub 검사와 Vercel·Cloud Run 상태를 확인하고 최신 `main` 전체 검증을 통과했다. C25는 공개 목록에 안정적인 `LATEST`·`OLDEST` 정렬과 워터마크 대표 사진 카드를 추가했고, C25A는 오류 응답 뒤 요청 트랜잭션 롤백과 Spring `ERROR` 디스패치를 보완했다. C26은 목록 카드를 `/posts/{postId}`로 연결하고 공통 본문·월 단위 방문 시기·장소명·Google 지도 링크와 현재 카테고리의 맛집 또는 여행지 전용 정보만 표시하는 반응형 상세 화면을 구현했다. 서버는 `PUBLIC + PUBLISHED`만 조회하고 검증된 현재 워터마크 `PUBLIC_DETAIL` 파생본만 별도 API로 스트리밍한다. 소유자 ID·내부 장소 ID·좌표·게시 일자·Storage 키와 비공개 파생본은 공개하지 않는다. 기존 관계와 인덱스를 재사용하므로 DB 마이그레이션은 없다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/post/service/PublicPostService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/PublicPostController.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPostDetailResponse.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPostPhotoResponse.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `frontend/src/app/posts/[postId]/page.tsx`, `frontend/src/domain/post/components/public-post-detail.tsx`, `frontend/src/domain/post/components/public-post-index.tsx`, `frontend/src/domain/post/api/public-post-api.ts`, `frontend/src/app/globals.css`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`
- 검증: 병합된 `main`에서 비밀정보·컨테이너 계약·프론트 Vercel/Sites 빌드·공개 산출물·백엔드 전체 검증을 통과했다. C25·C25A 대상 테스트에 이어 C26의 `PublicPostControllerTests`가 맛집·여행지 상세 분리, 비공개·링크 공개 차단, 안전한 `PUBLIC_DETAIL` 스트리밍과 비공개 파생본 제외를 확인했다. 프론트 lint·타입 검사와 네트워크 허용 환경의 Vercel 프로덕션 빌드가 통과했다.
- 결정: 카드와 상세 이미지 경로는 저장소 URL이 아닌 백엔드 공개 API 상대 경로만 반환한다. 상세 응답은 월 단위 방문 시기와 장소명·Google 지도 링크만 공개하고 좌표와 내부 식별자를 제외한다. 브라우저 우클릭 저장 억제는 보호 레이어와 드래그 방지로 보조하되 완전 차단으로 표현하지 않고, 원본 미제공·EXIF 제거·픽셀 워터마크를 실제 보호 경계로 유지한다.
- 위험: 공개 이미지 요청은 현재 Cloud Run을 거쳐 비공개 Supabase Storage를 읽으므로 트래픽 증가 시 CDN·서명 URL 또는 이미지 캐시 비용을 재검토해야 한다. 링크 공개 공유 주소와 같은 장소 반복 방문 화면은 아직 없다.
- 다음 작업: C26을 병합·배포해 실제 공개 게시물의 목록→상세 이동과 사진 표시를 확인한 뒤 C27 같은 장소의 반복 방문 기록을 구현한다.

## 日本語

- 目標: merge済みC24のproduction状態を検証してC25公開cover写真cardとC25A error response修正を引き渡し、C26 category別public detail pageを実装する。
- 完了: PR #43 merge commit `610f442`のGitHub検査とVercel・Cloud Run状態を確認し、最新`main`の全体検証を通過した。C25は公開listへ安定した`LATEST`・`OLDEST` sortとwatermark cover cardを追加し、C25Aはerror response後のrequest transaction rollbackとSpring `ERROR` dispatchを修正した。C26はlist cardを`/posts/{postId}`へ接続し、共通本文・月単位visit時期・place名・Google Maps linkと現在categoryのrestaurantまたはdestination専用情報だけを表示するresponsive detail画面を実装した。Serverは`PUBLIC + PUBLISHED`だけを取得し、検証済みcurrent watermark `PUBLIC_DETAIL` variantだけを別APIでstreamする。Owner ID・internal place ID・coordinate・publish日・Storage keyとprivate variantは公開しない。既存relationとindexを再利用するためDB migrationはない。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/post/service/PublicPostService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/PublicPostController.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPostDetailResponse.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPostPhotoResponse.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `frontend/src/app/posts/[postId]/page.tsx`, `frontend/src/domain/post/components/public-post-detail.tsx`, `frontend/src/domain/post/components/public-post-index.tsx`, `frontend/src/domain/post/api/public-post-api.ts`, `frontend/src/app/globals.css`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`
- 検証: merge済み`main`でsecret・container contract・frontend Vercel/Sites build・public artifact・backend全体検証を通過した。C25・C25A対象testに加えて、C26の`PublicPostControllerTests`がrestaurant・destination detail分離、private・unlisted遮断、安全な`PUBLIC_DETAIL` streamとprivate variant除外を確認した。Frontend lint・type検査とnetwork許可環境のVercel production buildが成功した。
- 決定: Cardとdetail image pathはStorage URLではなくbackend public APIのrelative pathだけを返す。Detail responseは月単位visit時期とplace名・Google Maps linkだけを公開し、coordinateとinternal identifierを除外する。Browser右クリック保存の抑制は保護layerとdrag防止による補助とし、完全遮断とは表現せず、original非提供・EXIF削除・pixel watermarkを実際の保護境界として維持する。
- リスク: Public image requestは現在Cloud Run経由でprivate Supabase Storageを読むため、traffic増加時はCDN・signed URLまたはimage cache費用を再検討する必要がある。Unlisted共有URLと同一place repeat visit画面はまだない。
- 次の作業: C26をmerge・deployして実際のpublic postのlist→detail遷移とphoto表示を確認後、C27同一place repeat visit historyを実装する。

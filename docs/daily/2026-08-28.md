# 2026-08-28 Daily Report

## 한국어

- 목표: 병합된 C24 운영 상태를 검증하고 C25 공개 대표 사진 카드와 최신순·오래된순 정렬을 pull request로 전달한다.
- 완료: PR #43 병합 커밋 `610f442`의 GitHub 검사와 Vercel·Cloud Run 상태를 확인하고 최신 `main` 전체 검증을 통과했다. C25는 공개 목록에 안정적인 `LATEST`·`OLDEST` 정렬을 추가하고 카테고리 전환 후에도 URL 쿼리로 정렬을 유지한다. 공개 카드는 대표 사진·카테고리·제목·한줄평·월 단위 방문 시기를 반응형으로 표시한다. 서버는 `PUBLIC + PUBLISHED` 게시물의 `READY` 대표 사진에서 검증된 공개 `MAP_CARD`만 목록에 포함하고 별도 공개 사진 API로 스트리밍한다. 원본·정제 마스터·Storage 키는 노출하지 않는다. 기존 인덱스와 제약을 재사용하므로 DB 마이그레이션은 없다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/post/service/PublicPostService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/PublicPostController.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPostCoverResponse.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `frontend/src/app/posts/page.tsx`, `frontend/src/domain/post/components/public-post-index.tsx`, `frontend/src/domain/post/components/public-post-sort-links.tsx`, `frontend/next.config.ts`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`
- 검증: 병합된 `main`에서 비밀정보·컨테이너 계약·프론트 Vercel/Sites 빌드·공개 산출물·백엔드 전체 검증을 통과했다. C25 대상 백엔드 테스트 7개는 비로그인 목록, 카테고리 합계, 최신순·오래된순, 검증된 워터마크 `MAP_CARD` 스트리밍, 비공개 자산 차단과 잘못된 쿼리 거부를 확인했다. 프론트 lint·타입 검사와 네트워크 허용 환경의 Vercel 빌드가 통과했다.
- 결정: 카드 이미지 경로는 저장소 URL이 아닌 백엔드 공개 API 상대 경로만 반환한다. 브라우저 우클릭 저장 억제는 보호 레이어와 드래그 방지로 보조하되 완전 차단으로 표현하지 않고, 원본 미제공·EXIF 제거·픽셀 워터마크를 실제 보호 경계로 유지한다.
- 위험: 공개 이미지 요청은 현재 Cloud Run을 거쳐 비공개 Supabase Storage를 읽으므로 트래픽 증가 시 CDN·서명 URL 또는 이미지 캐시 비용을 재검토해야 한다. C26 상세 화면과 링크 공개 공유 주소는 아직 없다.
- 다음 작업: C25를 병합·배포해 실제 공개 게시물의 워터마크 대표 사진과 두 정렬을 확인한 뒤 C26 카테고리별 공개 상세 페이지를 구현한다.

## 日本語

- 目標: merge済みC24のproduction状態を検証し、C25公開cover写真cardとnewest・oldest sortをpull requestとして引き渡す。
- 完了: PR #43 merge commit `610f442`のGitHub検査とVercel・Cloud Run状態を確認し、最新`main`の全体検証を通過した。C25は公開listへ安定した`LATEST`・`OLDEST` sortを追加し、category変更後もURL queryでsortを維持する。公開cardはcover写真・category・title・summary・月単位visit時期をresponsive表示する。Serverは`PUBLIC + PUBLISHED` postの`READY` cover写真から検証済み公開`MAP_CARD`だけをlistに含め、別のpublic photo APIでstreamする。Original・sanitized master・Storage keyは公開しない。既存indexとconstraintを再利用するためDB migrationはない。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/post/service/PublicPostService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/PublicPostController.java`, `backend/src/main/java/com/placesplates/domain/post/dto/PublicPostCoverResponse.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `frontend/src/app/posts/page.tsx`, `frontend/src/domain/post/components/public-post-index.tsx`, `frontend/src/domain/post/components/public-post-sort-links.tsx`, `frontend/next.config.ts`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`
- 検証: merge済み`main`でsecret・container contract・frontend Vercel/Sites build・public artifact・backend全体検証を通過した。C25対象backend test 7件は非login list、category count、newest・oldest sort、検証済みwatermark `MAP_CARD` stream、private asset遮断と不正query拒否を確認した。Frontend lint・type検査とnetwork許可環境のVercel buildが成功した。
- 決定: Card image pathはStorage URLではなくbackend public APIのrelative pathだけを返す。Browser右クリック保存の抑制は保護layerとdrag防止による補助とし、完全遮断とは表現せず、original非提供・EXIF削除・pixel watermarkを実際の保護境界として維持する。
- リスク: Public image requestは現在Cloud Run経由でprivate Supabase Storageを読むため、traffic増加時はCDN・signed URLまたはimage cache費用を再検討する必要がある。C26 detail画面とunlisted共有URLはまだない。
- 次の作業: C25をmerge・deployし、実public postのwatermark cover写真と2種類のsortを確認後、C26 category別public detail pageを実装する。

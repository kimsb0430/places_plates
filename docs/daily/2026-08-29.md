# 2026-08-29 Daily Report

## 한국어

- 목표: Sprint 5 C32·C33 운영 확인부터 Sprint 6 C34 이미지 보조 억제와 C35 공개 응답·보안 헤더 강화까지 완료한다.
- 완료: PC 지도 70%·축소 목록 30%, 모바일 지도 우선·가로 카드, 검색·현재 영역 제한과 마커·카드 양방향 선택을 구현했고 C33 지도 숫자를 자동 테스트로 고정했다. C34는 공개 사진의 우클릭·드래그·복사·모바일 길게 누르기를 억제했고, C34A는 Vercel 같은 출처 `/api/public-images/**` 중계와 운영 헤더 검사를 적용해 병합 후 스모크까지 통과했다. C35는 모든 프런트 경로에 Google Maps·Supabase 업로드 호환 CSP, 브라우저 권한·프레임·MIME 보호 헤더를 적용하고 Spring Boot 정상·인증 실패·오류 응답에도 API 전용 보안 헤더를 적용했다. 공개 목록·상세 JSON에서 소유자와 내부 Storage 키를 숨기는 회귀 테스트를 추가하고 예약 정리 실패 로그는 예외 종류만 남기도록 비식별화했다.
- 중요 파일: `frontend/next.config.ts`, `frontend/tests/unit/security-headers.test.ts`, `frontend/src/app/api/public-images/posts/[postId]/cover/route.ts`, `frontend/src/domain/map/components/map-split-explorer.tsx`, `frontend/src/domain/post/components/protected-public-image.tsx`, `backend/src/main/java/com/placesplates/global/security/SecurityConfig.java`, `backend/src/main/java/com/placesplates/domain/photo/service/TemporaryOriginalCleanupWorker.java`, `backend/src/test/java/com/placesplates/api/HealthControllerTests.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `backend/src/test/java/com/placesplates/domain/photo/service/TemporaryOriginalCleanupWorkerTests.java`, `scripts/check-production-image-protection.ps1`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 검증: C33 지도 합계, C34/C34A 이미지 억제·중계 헤더를 포함한 기존 검증을 통과했다. C35 프런트 보안 헤더 단위 테스트를 포함한 전체 9건과 Spring Boot 정상·인증 실패 응답 헤더, 공개 JSON 내부 키 비노출, 로그 메시지·스택 비기록 회귀 테스트를 통과했다. `scripts/verify-all.ps1`에서 비밀정보·Cloud Run 계약, 프런트 테스트·lint·타입 검사·Vercel/Sites 빌드·두 공개 산출물 검사와 전체 백엔드 테스트·JAR 빌드를 모두 통과했다. 샌드박스 안 Node 사용자 정보 조회는 `ENOMEM`으로 표시됐지만 같은 테스트를 샌드박스 밖에서 실행해 통과했다.
- 결정: 지도 SDK 수명과 탐색 UI 상태를 분리해 검색 입력마다 Dynamic Map을 다시 만들지 않는다. 지도 탭 전용 축소 카드만 추가하고 `/posts`의 대표 사진 큰 카드·정렬 UI는 변경하지 않는다. 검색을 시작하면 현재 영역 제한을 자동으로 해제해 화면 밖 일치 항목도 선택할 수 있게 한다. 클러스터 표시는 SDK가 넘긴 합계 대신 실제 포함 마커 배열로 숫자·범주·색상을 계산하며 같은 장소의 재방문도 각각 한 게시물로 센다. 외부 삽입 방지는 Cloud Run 직접 사진과 Vercel 같은 출처 Route Handler 양쪽에 `same-origin` 정책을 적용하고, 이미 최적화된 파생본은 Vercel 이미지 최적화 계층을 다시 거치지 않는다.
- 위험: 운영 지도 게시물이 한 건이라 복수 카드 전체 수동 시나리오는 공개 게시물이 추가된 뒤 확인해야 한다. C34 보조 억제는 개발자 도구·서버 프록시·화면 캡처를 막지 못한다. C35 CSP는 Google Maps 호환을 위해 공식 허용 목록의 `unsafe-inline`·`unsafe-eval`을 유지하므로 nonce 기반 엄격 CSP는 별도 성능·렌더링 검토 후 적용한다. 새 DB 마이그레이션과 필수 환경변수는 없다.
- 다음 작업: C35 PR 병합 후 Vercel 지도·장소 검색·Supabase 사진 업로드와 Cloud Run 정상·오류 응답 헤더를 운영에서 확인하고 C36 지도 지연 로딩·공개 이미지 성능 최적화를 진행한다.

## 日本語

- 目標: Sprint 5 C32・C33のproduction確認からSprint 6 C34 image補助抑制とC35 public response・security header強化まで完了する。
- 完了: PC map 70%・compact list 30%、mobile map-first・horizontal card、search・current bounds制限とmarker・card双方向選択を実装し、C33 map countを自動testで固定した。C34はpublic photoのcontext menu・drag・copy・mobile long pressを抑制し、C34AはVercel same-origin `/api/public-images/**` proxyとproduction header検査を適用してmerge後smokeまで通過した。C35は全frontend routeへGoogle Maps・Supabase upload互換CSP、browser permission・frame・MIME保護headerを適用し、Spring Bootの正常・authentication failure・error responseにもAPI専用security headerを適用した。Public list・detail JSONのowner・internal Storage key非公開regression testを追加し、scheduled cleanup failure logはexception typeだけを残すよう匿名化した。
- 重要ファイル: `frontend/next.config.ts`, `frontend/tests/unit/security-headers.test.ts`, `frontend/src/app/api/public-images/posts/[postId]/cover/route.ts`, `frontend/src/domain/map/components/map-split-explorer.tsx`, `frontend/src/domain/post/components/protected-public-image.tsx`, `backend/src/main/java/com/placesplates/global/security/SecurityConfig.java`, `backend/src/main/java/com/placesplates/domain/photo/service/TemporaryOriginalCleanupWorker.java`, `backend/src/test/java/com/placesplates/api/HealthControllerTests.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `backend/src/test/java/com/placesplates/domain/photo/service/TemporaryOriginalCleanupWorkerTests.java`, `scripts/check-production-image-protection.ps1`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 検証: C33 map count、C34/C34A image suppression・proxy headerを含む既存検証を通過した。C35 frontend security header unit testを含む全9件とSpring Boot normal・authentication failure response header、public JSON internal key非公開、log message・stack非記録regression testを通過した。`scripts/verify-all.ps1`でsecret・Cloud Run contract、frontend test・lint・type check・Vercel/Sites build・両public artifact scan、backend全test・JAR buildをすべて通過した。Sandbox内Node user情報取得は`ENOMEM`と表示されたが、同じtestをsandbox外で実行して通過した。
- 決定: Map SDK lifetimeとexplore UI stateを分離し、search入力ごとにDynamic Mapを再生成しない。Map tab専用compact cardだけを追加し、`/posts`のcover large card・sort UIは変更しない。Search開始時はcurrent bounds制限を自動解除し、画面外の一致項目も選択できるようにする。Cluster表示はSDKから渡される集計値ではなくactual included marker配列からcount・category・colorを計算し、same place repeat visitも各postを1件として数える。External embed防止はCloud Run direct写真とVercel same-origin Route Handlerの両方へ`same-origin` policyを適用し、既に最適化済みの派生画像はVercel image optimization layerへ再投入しない。
- リスク: Production map postは1件のためmultiple cardの全manual scenarioはpublic post追加後に確認する。C34補助抑制はdeveloper tools・server proxy・screen captureを防げない。C35 CSPはGoogle Maps互換のためofficial allowlistの`unsafe-inline`・`unsafe-eval`を維持するので、nonce-based strict CSPは別途performance・rendering検討後に適用する。新規DB migrationと必須environment variableはない。
- 次の作業: C35 PR merge後、Vercel map・place search・Supabase photo uploadとCloud Run正常・error response headerをproductionで確認し、C36 map lazy load・public image performance optimizationを進める。

# 2026-08-29 Daily Report

## 한국어

- 목표: Sprint 5 C32 지도 중심 분할 화면을 운영 확인하고 C33 지도 집계·필터·재방문 회귀 테스트를 구현한다.
- 완료: PC 지도 70%·축소 목록 30%, 980px 이하 지도 우선·가로 카드 레이아웃을 추가했다. 제목·장소 검색과 현재 영역 제한은 기존 지도 인스턴스를 유지한 채 마커·클러스터·카드를 갱신한다. 카드 선택은 마커로 이동해 정보창을 열고, 마커 선택은 카드를 선택·스크롤하며 hover·focus도 마커 강조와 연결한다. URL은 검색어·게시물 ID·지도 중심·확대 수준·지도 로드 상태를 보존하고 카테고리 링크가 이를 이어받는다. C33은 일반·날짜 변경선 영역 합계, 단일·혼합 클러스터 숫자와 색상, 전역·카테고리 지도 API 합계, 동일 장소 재방문 게시물 수와 공개 장소 이력 정렬을 자동 테스트로 고정했다.
- 중요 파일: `frontend/src/app/map/page.tsx`, `frontend/src/domain/map/components/map-split-explorer.tsx`, `frontend/src/domain/map/components/google-map-explorer.tsx`, `frontend/src/domain/map/components/map-category-tabs.tsx`, `frontend/src/domain/map/map-viewport-count.ts`, `frontend/src/domain/map/map-cluster-presentation.ts`, `frontend/tests/unit/map-counts.test.ts`, `backend/src/test/java/com/placesplates/api/PublicMapControllerTests.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `.github/workflows/verify.yml`, `scripts/verify-all.ps1`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 검증: 일반 경계·날짜 변경선 경계의 게시물 필터 assertion을 통과했다. C33 프런트 단위 테스트 3건과 관련 Spring API 통합 테스트, 전체 백엔드 회귀 테스트를 통과했다. `scripts/verify-all.ps1`에서 비밀정보·Cloud Run 계약, 프런트 단위 테스트·lint·타입 검사·Vercel/Sites 빌드·두 공개 산출물 검사와 전체 백엔드 테스트·JAR 빌드를 통과했다. 샌드박스 최초 Vercel 빌드의 Google Fonts 네트워크 실패는 네트워크 허용 재실행과 전체 검증에서 정상 통과했다.
- 결정: 지도 SDK 수명과 탐색 UI 상태를 분리해 검색 입력마다 Dynamic Map을 다시 만들지 않는다. 지도 탭 전용 축소 카드만 추가하고 `/posts`의 대표 사진 큰 카드·정렬 UI는 변경하지 않는다. 검색을 시작하면 현재 영역 제한을 자동으로 해제해 화면 밖 일치 항목도 선택할 수 있게 한다. 클러스터 표시는 SDK가 넘긴 합계 대신 실제 포함 마커 배열로 숫자·범주·색상을 계산하며 같은 장소의 재방문도 각각 한 게시물로 센다.
- 위험: 운영 지도 게시물이 한 건이라 복수 카드 스크롤, 검색 결과 클러스터 재구성, 마커에서 다른 카드로 이동하는 전체 수동 시나리오는 공개 게시물이 추가된 뒤 확인해야 한다. C33은 테스트·문서 작업으로 DB 마이그레이션과 새 환경변수가 없다.
- 다음 작업: C33 PR 병합과 운영 회귀 스모크를 확인한 뒤 C34 이미지 우클릭·드래그·외부 삽입 보조 억제를 구현한다.

## 日本語

- 目標: Sprint 5 C32 map-first split viewをproduction確認し、C33 map count・filter・repeat visit regression testを実装する。
- 完了: PC map 70%・compact list 30%、980px以下のmap-first・horizontal card layoutを追加した。Title・place searchとcurrent bounds制限は既存map instanceを維持したままmarker・cluster・cardを更新する。Card選択はmarkerへ移動してinfo windowを開き、marker選択はcardを選択・scrollし、hover・focusもmarker highlightへ接続する。URLはquery・post ID・map center・zoom・map load stateを保持し、category linkが引き継ぐ。C33はnormal・date line bounds count、single・mixed clusterのcountとcolor、global・category map API count、same place repeat visit post countとpublic place history sortを自動testで固定した。
- 重要ファイル: `frontend/src/app/map/page.tsx`, `frontend/src/domain/map/components/map-split-explorer.tsx`, `frontend/src/domain/map/components/google-map-explorer.tsx`, `frontend/src/domain/map/components/map-category-tabs.tsx`, `frontend/src/domain/map/map-viewport-count.ts`, `frontend/src/domain/map/map-cluster-presentation.ts`, `frontend/tests/unit/map-counts.test.ts`, `backend/src/test/java/com/placesplates/api/PublicMapControllerTests.java`, `backend/src/test/java/com/placesplates/api/PublicPostControllerTests.java`, `.github/workflows/verify.yml`, `scripts/verify-all.ps1`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 検証: Normal bounds・日付変更線boundsのpost filter assertionを通過した。C33 frontend unit test 3件と関連Spring API integration test、backend全regression testを通過した。`scripts/verify-all.ps1`でsecret・Cloud Run contract、frontend unit test・lint・type check・Vercel/Sites build・両public artifact scan、backend全test・JAR buildを通過した。Sandbox最初のVercel buildで発生したGoogle Fonts network failureはnetwork許可での再実行と全体検証で正常通過した。
- 決定: Map SDK lifetimeとexplore UI stateを分離し、search入力ごとにDynamic Mapを再生成しない。Map tab専用compact cardだけを追加し、`/posts`のcover large card・sort UIは変更しない。Search開始時はcurrent bounds制限を自動解除し、画面外の一致項目も選択できるようにする。Cluster表示はSDKから渡される集計値ではなくactual included marker配列からcount・category・colorを計算し、same place repeat visitも各postを1件として数える。
- リスク: Production map postは1件のため、multiple card scroll、search result cluster再構成、markerから別cardへの移動を含む全manual scenarioはpublic post追加後に確認する。C33はtest・document作業でDB migrationと新environment variableはない。
- 次の作業: C33 PR mergeとproduction regression smoke確認後、C34 image context menu・drag・external embed補助抑制を実装する。

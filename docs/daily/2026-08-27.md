# 2026-08-27 Daily Report

## 한국어

- 목표: C20·C21 운영 상태와 C22 사진 편집을 검증하고 C23 공개 범위·게시 전 안전 검사를 pull request로 전달한다.
- 완료: 여행지 전용 입력, Google Places·직접 입력 장소 연결, C22 사진 순서·대표 사진·대체 텍스트 저장의 운영 검증을 마쳤다. C23은 소유자 초안의 비공개·링크 공개·전체 공개 선택, 9개 게시 준비 항목 조회와 CSRF 보호 게시 API를 추가했다. 서버는 제목·방문 월·한줄평·장소·사진·대표 사진뿐 아니라 모든 사진 `READY`, 업로드 원본 삭제 완료, 정제 마스터의 비공개·메타데이터 검사, 세 공개 파생본의 현재 워터마크 버전·위치를 요청 때마다 다시 확인한다. 모든 조건을 통과할 때만 선택 범위의 `PUBLISHED`와 게시 시각을 저장한다. 기존 V1·V12·V13 컬럼을 사용하므로 DB 마이그레이션은 없다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/post/service/DraftPublicationService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/DraftPostController.java`, `backend/src/main/java/com/placesplates/domain/post/entity/DraftPost.java`, `backend/src/test/java/com/placesplates/api/DraftPublicationControllerTests.java`, `frontend/src/domain/post/components/draft-publication-panel.tsx`, `frontend/src/domain/post/components/draft-post-view.tsx`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`
- 검증: C22 운영에서 사진 순서·대표 사진·대체 텍스트가 새로고침 후 유지됨을 확인했다. C23 대상 통합 테스트는 미완료 게시 차단, 안전한 링크 공개 상태 전환, 인증·CSRF 경계를 확인했다. 전체 저장소 검증으로 비밀정보 검사, Cloud Run 컨테이너 계약, 프론트 lint·타입 검사·Vercel/Sites 빌드와 두 공개 산출물 검사, 백엔드 전체 테스트·배포 JAR 생성을 확인했다.
- 결정: 게시 준비 결과는 브라우저 상태를 신뢰하지 않고 서버가 저장된 DB 상태로 다시 계산한다. C17에서 저장 바이트를 깊게 검증한 결과를 C23이 원본 삭제·메타데이터·워터마크 정책 상태와 함께 재검사한다. 링크 공개의 추측 불가능한 공유 주소와 실제 읽기 화면은 C24~C27 공개 조회 작업에서 연결한다.
- 위험: C23은 게시 상태 전환까지 제공하지만 공개 목록·상세 페이지와 링크 공유 주소는 아직 연결되지 않았다. 운영에서는 C24 이후 공개 조회 경로가 준비되기 전까지 중요한 초안을 시험 게시하지 않는다.
- 다음 작업: C23을 병합·배포해 게시 준비 항목 표시를 확인한 뒤 C24 전체·맛집·여행지 공개 목록 API와 탭별 합계를 구현한다.

## 日本語

- 目標: C20・C21のproduction状態とC22写真編集を検証し、C23公開範囲・publish前安全検査をpull requestとして引き渡す。
- 完了: Destination専用入力、Google Places・manual場所接続、C22写真順序・cover写真・alt text保存のproduction検証を完了した。C23はowner draftのprivate・unlisted・public選択、9項目のpublish readiness照会とCSRF保護publish APIを追加した。Serverはtitle・visit month・summary・place・写真・coverだけでなく、全写真の`READY`、upload原本削除完了、sanitized masterのprivate・metadata検査、3つのpublic variantの現行watermark version・positionをrequestごとに再確認する。全条件を通過した場合だけ選択範囲の`PUBLISHED`とpublish時刻を保存する。既存V1・V12・V13 columnを利用するためDB migrationはない。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/post/service/DraftPublicationService.java`, `backend/src/main/java/com/placesplates/domain/post/controller/DraftPostController.java`, `backend/src/main/java/com/placesplates/domain/post/entity/DraftPost.java`, `backend/src/test/java/com/placesplates/api/DraftPublicationControllerTests.java`, `frontend/src/domain/post/components/draft-publication-panel.tsx`, `frontend/src/domain/post/components/draft-post-view.tsx`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`
- 検証: C22 productionで写真順序・cover写真・alt textがreload後も維持されることを確認した。C23対象integration testは未完了publish拒否、安全なunlisted状態遷移、認証・CSRF境界を確認した。Repository全体検証でSecret検査、Cloud Run container contract、frontend lint・type検査・Vercel/Sites buildと2つのpublic artifact検査、backend全test・配備JAR生成を確認した。
- 決定: Publish readinessはbrowser状態を信頼せず、serverが保存済みDB状態から再計算する。C17で保存byteをdeep検証した結果をC23が原本削除・metadata・watermark policy状態とともに再検査する。Unlistedの推測不能な共有URLと実際のread画面はC24〜C27公開照会作業で接続する。
- リスク: C23はpublish状態遷移まで提供するが、public list・detail pageとunlisted共有URLはまだ接続されていない。ProductionではC24以降のpublic read経路が準備されるまで重要なdraftをtest publishしない。
- 次の作業: C23をmerge・deployしてpublish readiness表示を確認した後、C24全体・restaurant・destination public list APIとtab別countを実装する。

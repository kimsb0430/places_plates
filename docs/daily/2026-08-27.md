# 2026-08-27 Daily Report

## 한국어

- 목표: C20 여행지 전용 입력의 병합 후 상태를 검증하고 C21 Google Places·직접 입력 장소 연결을 pull request로 전달한다.
- 완료: 여행지 초안에 추천 방문 시간·소요시간·볼거리·여행 팁 선택 입력을 추가했다. Spring Boot API는 `destination_details`를 소유자 초안 범위에서 조회·저장하고, 모든 전용 값이 비면 상세 행을 삭제하며, 맛집 게시물의 여행지 전용 요청을 거부한다. Next.js 편집 화면은 여행지에서만 네 항목을 표시하고 기존 700ms CSRF 보호 자동 저장에 포함한다. C21은 인증된 Places API (New) 텍스트 검색, Google Maps 링크, Google·직접 입력 장소의 초안 연결·해제를 추가했다. 검색은 버튼을 누를 때만 최대 5건을 요청하고 결과 컨테이너에 Google Maps 출처를 표시한다. 검색 키는 Spring Boot에만 두며, 검색 실패·미설정 시 장소명과 선택 좌표 쌍을 직접 저장할 수 있다. 기존 V1 테이블을 사용하므로 DB 마이그레이션은 없다.
- 중요 파일: `backend/src/main/java/com/placesplates/domain/place/`, `backend/src/main/java/com/placesplates/infra/googlemaps/GooglePlacesSearchClient.java`, `backend/src/main/java/com/placesplates/domain/post/service/DraftPostService.java`, `backend/src/test/java/com/placesplates/api/PlaceControllerTests.java`, `frontend/src/domain/post/components/place-fields.tsx`, `frontend/src/domain/post/components/draft-post-view.tsx`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 검증: 대상 백엔드 통합 테스트와 전체 저장소 검증이 통과했다. C21은 Google 요청의 키·필드 마스크·5건 제한 응답 매핑과 키 미설정 오류, 인증 검색, Google·직접 입력 연결, 불완전 좌표 거부, 연결 해제를 자동 테스트했다. 비밀정보 검사, Cloud Run 컨테이너 계약, 프론트 lint·타입 검사·Vercel/Sites 빌드와 두 공개 산출물 검사, 백엔드 전체 테스트·배포 JAR 생성을 확인했다. 1440px과 390px 로컬 렌더링에서 전용 필드 4개 표시와 가로 넘침 없음, 모바일 1열 배치를 확인했다.
- 결정: 추천 방문 시간은 100자, 볼거리와 여행 팁은 각각 5,000자로 제한하며 소요시간은 0분 이상의 정수 분으로 저장한다. Google Places 검색 키와 향후 Maps JavaScript 브라우저 키를 분리한다. Place ID는 장기 보관하고 API 좌표 스냅샷에는 `refreshed_at`을 기록해 30일 갱신 정책을 C29 지도 조회에 적용한다.
- 위험: C21 운영 검색에는 Google Cloud에서 Places API (New)를 활성화하고 Cloud Run에 `GOOGLE_PLACES_API_KEY` Secret을 연결해야 한다. 검색 결과의 장소 스냅샷 갱신과 만료 좌표 배제는 공개 지도를 구현하는 C29에서 완성해야 한다. 공개 게시와 공개 이미지 제공은 C23까지 구현 전이다.
- 다음 작업: C22 사진 순서·대표 사진·대체 텍스트 편집을 구현한다.

## 日本語

- 目標: C20のmerge後状態を検証し、C21 Google Places・manual場所接続をpull requestとして引き渡す。
- 完了: Destination draftへrecommended visit time・duration・highlights・travel tipsのoptional inputを追加した。Spring Boot APIは`destination_details`をowner draft範囲で照会・保存し、全専用値が空ならdetail rowを削除し、restaurant postへのdestination専用requestを拒否する。Next.js editorはdestinationだけに4項目を表示し、既存の700ms CSRF保護autosaveへ含める。C21は認証済みPlaces API (New) text search、Google Maps link、Google・manual場所のdraft接続・解除を追加した。検索buttonを押した時だけ最大5件を要求し、result container内にGoogle Maps attributionを表示する。検索keyはSpring Bootだけへ保存し、検索失敗・未設定時は場所名とoptional座標pairを直接保存できる。既存V1 tableを利用するためDB migrationはない。
- 重要ファイル: `backend/src/main/java/com/placesplates/domain/place/`, `backend/src/main/java/com/placesplates/infra/googlemaps/GooglePlacesSearchClient.java`, `backend/src/main/java/com/placesplates/domain/post/service/DraftPostService.java`, `backend/src/test/java/com/placesplates/api/PlaceControllerTests.java`, `frontend/src/domain/post/components/place-fields.tsx`, `frontend/src/domain/post/components/draft-post-view.tsx`, `PLACES_AND_PLATES_PRODUCT_DESIGN.html`, `PROJECT_STRUCTURE.md`, `DEVELOPMENT_SCHEDULE.md`, `docs/DATABASE_SCHEMA.md`, `docs/DEPLOYMENT_CHECKLIST.md`
- 検証: 対象backend integration testとrepository全体検証が成功した。C21はGoogle requestのkey・field mask・5件制限response mappingとkey未設定error、認証検索、Google・manual接続、不完全座標拒否、接続解除を自動testした。Secret検査、Cloud Run container contract、frontend lint・type検査・Vercel/Sites buildと2つの公開artifact検査、backend全test・配備JAR生成を確認した。1440pxと390pxのlocal renderで専用4項目、横overflowなし、mobile 1列配置を確認した。
- 決定: Recommended visit timeは100文字、highlightsとtravel tipsは各5,000文字に制限し、durationは0以上の整数分で保存する。Google Places server keyと将来のMaps JavaScript browser keyを分離する。Place IDは長期保存し、API座標snapshotには`refreshed_at`を記録して30日更新policyをC29 map queryへ適用する。
- リスク: C21 production searchにはGoogle CloudでPlaces API (New)を有効化し、Cloud Runへ`GOOGLE_PLACES_API_KEY` Secretを接続する必要がある。検索resultの場所snapshot更新と期限切れ座標除外は公開mapを実装するC29で完了する。公開postと公開image配信はC23まで未実装である。
- 次の作業: C22写真順序・cover写真・alt text編集を実装する。

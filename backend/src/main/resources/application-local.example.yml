spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

places-plates:
  google-maps:
    api-key: ${GOOGLE_MAPS_API_KEY}
  jwt:
    secret: ${JWT_SECRET}
